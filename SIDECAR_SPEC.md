# サイドカーファイル関連付け仕様

更新日: 2026-07-27

## 1. 対象と設定

「同名のサイドカーファイルを画像に関連付ける」は既定でOFFとし、対象拡張子とともに設定ファイルへ保存する。
対象拡張子の既定値は `.xmp` とする。UIではセミコロン、カンマ、または空白区切りで複数指定でき、先頭の `.` は省略可能とする。保存時は先頭に `.` を付けた小文字へ正規化し、大小文字を区別せず重複を除く。
ONの場合は「画像・動画以外のファイルも含める」がOFFでも、設定した拡張子の関連候補をスキャン対象へ加える。
両方がONの場合、関連付けできないサイドカー候補とその他ファイルは独立ファイルとして扱う。

空の拡張子、複合拡張子、空白・パス区切り・ワイルドカード等を含む拡張子は指定できない。画像、RAW、動画として認識する拡張子も、自己関連付けを防ぐため指定できない。
既存の設定ファイルに対象拡張子がない場合は `.xmp` を使用する。

## 2. 関連先の決定

同一コピー元ディレクトリ内で次の順に判定する。

1. `photo.jpg.xmp` と `photo.jpg`、`photo.nef.pp3` と `photo.nef` のような画像ファイル名全体の一致
2. `photo.xmp` や `photo.aae` と、サイドカー拡張子を除いた名前が一致する画像が1件だけ
3. `photo.xmp` や `photo.aae` に同名RAWとJPEGが各1件だけある場合はRAW
4. 上記で一意にならなければ関連付けず、警告する

JPEG、RAW、その他画像を関連先候補とし、動画と非画像は候補にしない。

## 3. コピー先

画像のテンプレート評価と `{Sequence}` を先に確定する。サイドカーへテンプレートを再評価しない。

- `photo.xmp` や `photo.aae` は、画像の確定先の拡張子をサイドカー拡張子へ置き換える
- `photo.jpg.xmp` や `photo.nef.pp3` は、画像の確定ファイル名全体へサイドカー拡張子を付ける
- サイドカーの一覧行には画像のコピー元相対パスを「関連先」として表示する

## 4. 連番と上書き

`{Sequence}` の所有者は画像とする。コピー元サイドカーの一致・不一致によって画像の連番を変更しない。

- 画像が同一なら、その画像の連番へ固定する
- 画像が異なる場合は既存の `{Sequence}` 規則で次の画像候補へ進む
- 確定したサイドカーが不一致で上書きOFFならサイドカー競合とする
- 確定したサイドカーが不一致で上書きONならサイドカーだけ上書きする
- 未取込画像に対するサイドカー競合または読取エラーは、誤関連付けを防ぐため画像もコピー不能にする
- 取込済み画像に対するサイドカー競合は画像を増やさず、サイドカーだけ競合とする

コピー元に対応サイドカーがなく、コピー先の画像候補名に設定対象のサイドカーだけが存在する場合、そのファイルを孤立サイドカーとして保護する。設定されたすべての拡張子について、ベース名方式と画像ファイル名全体方式の両方を確認する。

- 画像が同一なら連番を固定し、孤立サイドカーを変更しない
- 画像が存在しなければ候補名を予約済みとして次の連番へ進む
- `{Sequence}` がなければ競合とする
- 上書きONでも、コピー元に対応ファイルがない孤立サイドカーを削除・上書きしない

## 5. 選択とコピー順

- 未取込画像の選択を解除すると、その関連サイドカーも解除する
- 未取込サイドカーを対象にするには、未取込画像も対象でなければならない
- 画像が取込済みの場合は、サイドカーだけをコピーできる
- コピー計画では画像を先に実行する
- 画像コピーが失敗した場合、依存するサイドカーは未実行エラーとする
- サイドカーコピーが失敗してもコピー済み画像を削除しない
- 次回スキャンでは画像を同じ連番の取込済みとして認識し、サイドカーだけを再試行できる

## 6. 安全性

コピー元・コピー先のスナップショット検証、一時ファイル、書き込みフラッシュ、正式名確定、コピー後検証は通常ファイルと同じ規則を使う。
孤立サイドカーの自動削除、移動、名前変更は行わない。
