# 設定プリセット仕様

- 作成日: 2026-08-02
- 最終更新日: 2026-08-02
- 対象バージョン: v1
- 状態: 未実装（設計のみ）

## 1. 目的

コピー元、コピー先、テンプレート、その他のスキャン・コピー条件を名前を付けて保存し、後から呼び出せるようにする。

`settings.xml` に保存している「前回の状態」は引き続き1件だけ保持し、起動時に自動復元する。プリセットはそれとは別に、利用者が明示的に作成・選択する複数件の設定セットとする。

DESIGN.md 開発ステップ9の「テンプレートプリセット」は本仕様へ統合し、テンプレート単独のプリセット機構は設けない。

## 2. 保存する項目

保存対象の判断基準は次の2点とする。設定項目が増えた場合もこの基準で振り分ける。

- **スキャン結果・コピー先・コピー可否を変える設定**はプリセットへ含める
- **マシン環境と画面表示の好み**はプリセットへ含めず、`settings.xml` のアプリ全体設定として保持する

### 2.1 プリセットへ含める項目

| 項目 | 備考 |
|---|---|
| コピー元フォルダ | 任意保存。既定は保存しない。§3 を正とする |
| コピー先フォルダ | 絶対パスを保存する |
| テンプレート | [TEMPLATE_SPEC.md](TEMPLATE_SPEC.md) の構文をそのまま保存する |
| 既存ファイルを上書きする | |
| 対象ファイル列挙モード | `MediaOnly` / `AllFiles` |
| サイドカー関連付けの有無と対象拡張子 | [SIDECAR_SPEC.md](SIDECAR_SPEC.md) の正規化規則を適用して保存する |
| RAW+JPEGペアではJPEGのみ解析する | RAW行のExif値、すなわち宛先パスを変えるため対象とする |
| テンプレートで未使用の場合もExif情報を読み込む | スキャンの読込挙動を変えるため対象とする |

### 2.2 プリセットへ含めない項目

| 項目 | 除外する理由 |
|---|---|
| Exif キャッシュを利用する | 性能最適化であり、マシンの環境に属する |
| Exif キャッシュの保存先、以前の保存先 | 他PCの絶対パスを持ち込む事故になる。エクスポート時に特に危険 |
| 画像プレビューを表示 | 画面表示の好みであり、取り込み内容と関係しない |

### 2.3 将来の追加候補

- 一覧フィルター条件。[FILTER_SPEC.md](FILTER_SPEC.md) の型付き条件モデルは永続化自体が未実装のため、v1 では対象外とする。プリセット要素へ後から `<Filter>` を追加できる形だけを確保する
- 「絞り込みで非表示になった項目のチェックを外す」設定

## 3. コピー元フォルダの扱い

コピー元はカードの挿抜でドライブレターが変わるため、他の項目と同じには扱わない。

- v1 では**絶対パスの任意保存**とする。「名前を付けて保存」時の「コピー元フォルダーも保存する」チェックで指定し、既定はOFFとする
- コピー元を保存していないプリセットを呼び出した場合、現在のコピー元フォルダは変更しない。空にもしない
- 保存済みのコピー元が存在しない場合も、値はそのまま入力欄へ適用し、既存の入力検証でエラーを表示する。§5.2 を正とする
- 将来案として、ボリュームシリアル番号とボリュームルートからの相対パスで保存し、呼び出し時にドライブレターを再解決する方式を検討する。既存の `GetVolumeInformationW` / `GetVolumePathNameW` とカード管理画面の実装を再利用できる。v1 のスキーマでは `<SourceVolume>` 要素を予約するだけとし、書き出さない

## 4. 保存場所とファイル形式

### 4.1 保存場所

`%LocalAppData%\PhotoImporter\presets.xml` とし、`settings.xml` とは別ファイルにする。

- `settings.xml` は「前回の状態」、`presets.xml` は利用者が育てる資産であり、性質が異なる。分離することで、片方の破損がもう片方を巻き込まない
- エクスポート・インポート、別PCへの持ち出しがファイル単位で完結する

### 4.2 形式

```xml
<PhotoImporterPresets version="1">
  <Preset id="01234567-89ab-cdef-0123-456789abcdef" name="旅行RAW取込"
          createdUtc="2026-08-02T01:23:45.6789012Z"
          updatedUtc="2026-08-02T01:23:45.6789012Z"
          lastUsedUtc="2026-08-02T01:23:45.6789012Z">
    <SourceFolder saved="false" />
    <DestinationFolder>D:\Photos</DestinationFolder>
    <TemplateText>{TakenDate:yyyy-MM-dd}\{FileName}{Sequence}{Extension}</TemplateText>
    <OverwriteExisting>false</OverwriteExisting>
    <SourceFileSelectionMode>MediaOnly</SourceFileSelectionMode>
    <AssociateSidecars>true</AssociateSidecars>
    <SidecarExtensions>
      <Extension>.xmp</Extension>
    </SidecarExtensions>
    <AnalyzeJpegOnlyForRawJpegPair>true</AnalyzeJpegOnlyForRawJpegPair>
    <ReadExifInformation>false</ReadExifInformation>
  </Preset>
</PhotoImporterPresets>
```

- 文字コードは UTF-8 BOM 付き、改行は CRLF とし、`settings.xml` と揃える
- `id` は `Guid.ToString("D")` 相当のハイフン付き形式で書き出す。読込時は大文字・小文字を区別しない
- 日時属性は `yyyy-MM-ddTHH:mm:ss.fffffffZ` の UTC とし、`InvariantCulture` で往復する
- `createdUtc` は新規作成時、`updatedUtc` は内容または名前を変更した時、`lastUsedUtc` は適用した時に更新する
- 未使用のプリセットでは `lastUsedUtc` 属性を省略する。「名前を付けて保存」で作成したプリセットは、保存後に現在設定と結び付くため、作成時刻を `lastUsedUtc` とする
- コピー元を保存する場合は `<SourceFolder saved="true">E:\DCIM</SourceFolder>` とする
- 通常の保存では、保存前の共通検証に成功したサイドカー拡張子を正規化して書き出す。インポートまたは既存ファイルから読み込んだ意味上の不正値は、修正されるまで正規化や既定値への置換をせず元の文字列を保持する
- `<SourceVolume>` は §3 の将来案のために予約する。v1 では書き出さず、存在しても無視する
- ルート要素の `version` が未対応のファイルは読み込まない。ただし破損とは扱わず、退避・上書きをせずに元の場所へ残す。詳細は §4.4 に従う

### 4.3 識別とプリセットの制約

- `id` は GUID とし、リネームしても同一性を保つ。`settings.xml` に保持する「最後に適用したプリセット」の参照はこの `id` で行う
- `name` は表示名とし、前後の空白を除去した後の長さを 1～100 文字とする。制御文字は許可しない
- `name` は `OrdinalIgnoreCase` で一意とする。Windows のファイル名に使用できない文字は表示名では許可し、エクスポート時の既定ファイル名だけを安全な文字へ置換する
- 件数の上限を 100 件とする。上限に達した状態での新規保存はエラーとして表示し、既存プリセットを自動削除しない
- ファイル内の並び順は作成順とし、UI 上の表示順は §5.3 に従う

### 4.4 保存と読み込みの信頼性

- 保存は既存の `PhotoImporterSettingsStore` と同じく、同一フォルダの一意な一時ファイルへ書き出し、`File.Replace` で原子的に確定する
- アプリの複数起動を許可しているため、すべての更新操作は**名前付き Mutex を取得 → ファイルを再読込 → 対象プリセットだけを更新 → 保存 → 解放**の順で行う。ファイル全体の後勝ち上書きによって、別インスタンスが追加したプリセットを消さない
- 更新操作には、新規保存、上書き、リネーム、複製、削除、インポート、`lastUsedUtc` 更新、破損ファイルの退避と再作成を含める
- Mutex 名は正規化した `presets.xml` の絶対パスの SHA-256 ハッシュから決定的に生成し、ユーザーセッション内をスコープとする。既定の待機時間は 10 秒とする
- 放棄された Mutex を取得した場合は取得成功として扱うが、処理前に必ずファイルを再読込する
- Mutex を取得できない場合、保存は失敗として表示する。成功扱いにして UI の一覧だけを更新しない
- Mutex 取得後の再読込で更新対象の `id` が存在しない場合は、他のインスタンスで削除されたものとして処理を失敗させ、一覧を再読込する。削除済みプリセットを暗黙に復活させない
- 読み込みに失敗した場合は例外で起動を止めない。プリセットは無くてもアプリが動作する補助データであり、`settings.xml` とは扱いを変える。警告を表示して空の一覧で継続する
- XML の破損、ルート要素の不正、現行バージョンで必須となる要素・属性の欠落、GUID・日時・真偽値・列挙値の形式不正は構造上の破損とする。読み込めなかったファイルは、Mutex 内で再読込して同じ破損を確認した後、`presets.bad.xml` へ退避してから新しいファイルを作成する。退避先が既にある場合は上書きせず、連番を付けた名前にする
- アクセス拒否、共有違反などにより退避できない場合は新しい `presets.xml` を上書き作成しない。元ファイルを残したまま、警告と空の一覧で継続する
- 数値として解釈できるが現行アプリが対応していない `version` は、新旧を問わず破損ではない。対応する明示的な移行処理がない場合はファイルを移動・変更せず、未対応バージョンであることを警告して空の一覧で継続する
- 存在しないフォルダ、不正なテンプレート、不正なサイドカー拡張子は XML の構造上の破損としない。文字列を失わずプリセットへ保持し、適用時の検証対象とする
- 将来追加された未知の要素・属性は現在の設定へ適用しないが、ルートと各プリセットの拡張データとして保持し、再保存時にも残す。既知の要素・属性と衝突する場合は既知の値を正とする。空白や属性順など意味を持たない書式の完全一致は求めない

## 5. UI

### 5.1 プリセットバー

メインウィンドウ最上段（現在未使用の `Grid.Row="0"`）に1行で配置する。

```
プリセット [ 旅行RAW取込        ▼ ] [保存] [名前を付けて保存...] [管理...]   ● 未保存の変更
```

- ComboBox の選択でプリセットを**即時適用**する。選択と適用を分けた2アクション方式は主要動線としては煩雑なため採用しない。代わりに次の安全策を設ける
  1. スキャン・コピー中は `CanEditSettings` によりバー全体を無効化する。既存の設定入力欄と同じ扱いとする
  2. `PreviewMouseWheel` を処理してマウスホイールによる選択変更を抑止する
  3. 現在の設定が基準値と異なる場合だけ確認ダイアログを表示し、「適用」「保存してから適用」「キャンセル」から選ばせる。WPF 標準 `MessageBox` の固定ボタン名は使用せず、3つの操作名を明示できる専用ダイアログを使用する
- 基準値は、プリセット選択中はそのプリセット、未選択時は起動時に `settings.xml` から復元した設定または未選択へ移行した時点の設定とする
- プリセット未選択時も、基準値から変更された場合は「(プリセットなし) ● 未保存の変更」と表示する
- 適用直前の設定値、選択プリセットの `id`、未保存状態をメモリに1件保持し、適用後に「プリセットを適用しました [元に戻す]」を表示する。元に戻してもファイルと `lastUsedUtc` は変更しない
- 「元に戻す」は、プリセット対象項目の編集、別プリセットの適用、保存、名前を付けて保存、スキャン開始、アプリ終了のいずれかで失効する
- 差分判定は §2.1 の項目のみを対象とする。パスは `Path.GetFullPath` 相当で絶対パス化し、末尾の区切り文字を除いて `OrdinalIgnoreCase` で比較する。テンプレートは `Ordinal`、真偽値と列挙値は値、サイドカー拡張子は正規化後の `OrdinalIgnoreCase` の集合として比較する
- コピー元を保存していないプリセットでは、コピー元を差分判定に含めない。コピー元を保存する指定なのに現在値が空の場合は保存エラーとする
- **[保存]** は選択中のプリセットへ上書きし、`updatedUtc` を更新して未保存表示を消す。`lastUsedUtc` は変更しない。プリセット未選択の場合は「名前を付けて保存」と同じ動作にする
- **[名前を付けて保存...]** は名前の入力と「コピー元フォルダーも保存する」チェックを求める。既存の名前と一致する場合は上書き確認を表示する。成功後は作成したプリセットを選択状態にし、未保存表示を消して `lastUsedUtc` を作成時刻に設定する
- 「保存してから適用」で保存に失敗した場合は、選択変更と適用を行わない
- **[管理...]** は §5.3 の管理オーバーレイを開く
- `AutomationProperties.AutomationId` を `PresetSelector`、`SavePreset`、`SavePresetAs`、`OpenPresetManager`、`PresetDirtyIndicator` として付与する

### 5.2 適用時の動作

- 適用は現在のスキャン結果を無効化し、再スキャンを促す。テンプレート変更時と同じ扱いとする
- スキャン中・コピー中は適用しない。バーが無効のため操作自体が到達しない
- 存在しないフォルダ、不正なテンプレート、不正なサイドカー拡張子、コピー元とコピー先の包含関係違反があっても、値はそのまま入力欄へ適用する。黙って捨てたり、黙って既定値へ落としたりしない
- 適用直後に現在設定の共通検証を実行し、サイドカー拡張子は既存のインライン表示、フォルダ・テンプレート・包含関係はメインのメッセージ領域へ表示する。同じ共通検証をスキャン開始時にも使用する
- 現在設定に検証エラーがある場合、「保存」と「名前を付けて保存」は失敗としてエラーを表示する。既に読み込まれている不正なプリセットは、修正されるまで上書き保存できないが、削除とエクスポートは可能とする
- 不正なサイドカー拡張子を含むプリセットの適用後に終了する場合は、終了自体を無条件に禁止しない。「修正する」「現在の状態を `settings.xml` へ保存せず終了」から選択できるようにする
- 適用に成功したプリセットの `lastUsedUtc` を更新する。更新の保存に失敗した場合も適用自体は成功として扱い、警告のみ表示する

### 5.3 管理オーバーレイ

Exif設定・フィルター条件と同じくオーバーレイとして実装し、別ウィンドウのモーダルにはしない。既存2画面と操作感を揃える。

- プリセットの一覧を表示する。表示項目は名前、コピー先、テンプレート、最終利用日、コピー元の保存有無
- 選択したプリセットの全項目を読み取り専用で表示する
- 操作: 名前の変更、複製、削除、表示順の切り替え、エクスポート、インポート
- 削除は確認を求める。確認文には「削除してもコピー済みの写真と設定ファイルの現在値は変わらない」ことを明記する
- 選択中のプリセットを削除した場合は現在の設定値を変更せず、選択と `settings.xml` の「最後に適用したプリセット」の参照を解除する。削除時点の現在設定を未選択状態の新しい基準値とし、未保存表示は出さない。削除は「元に戻す」の対象としない
- エクスポートは選択したプリセットを §4.2 と同じ形式の単独ファイルへ書き出す。インポートしたプリセットは自動適用しない。詳細は §5.5 に従う
- 一覧の表示順は名前の昇順（`OrdinalIgnoreCase`）を既定とする。最終利用日の降順へ切り替えられるようにする。最終利用日がないプリセットは末尾に置き、同値の場合は名前の昇順とする
- v1 では手動並べ替えを設けない。ファイル内の作成順は変更せず、「表示順」は管理画面上の並び方だけを意味する
- テンプレートだけを差し替えたい場合に備え、選択したプリセットから「テンプレートだけ取り込む」操作を提供する
- 管理オーバーレイを開いている間はプリセットバーと背面の設定操作を無効化する。`Escape` で閉じ、閉じた後は開いたボタンへフォーカスを戻す。確認中は管理オーバーレイ自体も操作不可とする
- 一覧の再読込で選択項目が無くなった場合は詳細欄を空表示にする

### 5.4 起動時の動作

- 起動時にプリセットを自動適用しない。`settings.xml` からの「前回の状態」復元は現行どおりとする
- `settings.xml` に「最後に適用したプリセットの `id`」を追加し、起動時に ComboBox の選択と未保存表示を復元する
- 参照先のプリセットが削除済みの場合はプリセット未選択として扱い、エラーにしない
- 「最後に適用したプリセットの `id`」は既存の前回状態と同じく正常終了時に `settings.xml` へ保存する。プリセット適用のたびには書き込まない
- 複数起動間の変更を反映するため、ComboBox を開く時、管理オーバーレイを開く時、保存・削除・インポートなどの更新操作後に `presets.xml` を再読込する。常時のファイル監視は行わない
- 再読込で選択中プリセットが他のインスタンスにより削除されていた場合は、現在設定を維持したまま未選択へ移行し、その設定を新しい基準値とする

### 5.5 エクスポートとインポート

- エクスポートは1ファイルにつき1プリセットとする。既定ファイル名はプリセット名から Windows のファイル名に使用できない文字を置換して作り、拡張子を `.xml` とする。既存ファイルを上書きする場合は確認する
- エクスポート元のプリセットと `presets.xml` は変更しない。§2.2 のマシン固有設定は出力しない
- インポート元ファイルは変更・移動しない。未対応の `version` はエラーとして取り込まず、外部ファイルであるため `.bad.xml` への退避も行わない
- インポートは1件を1トランザクションとして扱い、XML 構造、名前、ID、件数などの検証または保存に失敗した場合は追加・上書きを一切確定しない
- 存在しないフォルダ、不正なテンプレート、不正なサイドカー拡張子はインポート失敗の理由にしない。値をそのまま取り込み、管理画面で警告を表示し、適用時に §5.2 の共通検証へ出す
- 名前も `id` も重複しない場合は新規追加する。インポート元の `createdUtc` と `updatedUtc` は維持し、`lastUsedUtc` は削除する
- 名前が重複して「上書き」を選んだ場合は、ローカル側の `id` と `createdUtc` を維持し、内容を置き換えて `updatedUtc` を現在時刻へ更新する
- 「別名で追加」または複製では新しい `id` と `createdUtc` を発行し、`lastUsedUtc` は設定しない
- 名前は異なるが `id` が重複する場合は、同一プリセットの候補として上書き確認を表示する。暗黙に新規追加しない
- 「上書き」「別名で追加」「取り込まない」の選択後も、100件上限と名前の一意性を最終的に検証してから保存する

## 6. 実装の段階

1. **コア**: プリセットのモデルとストアを `PhotoImporter.Core.Settings` へ実装し、単体テストを追加する
2. **UI 第1段階**: プリセットバー（呼び出し、保存、名前を付けて保存、未保存表示、元に戻す）
3. **UI 第2段階**: 管理オーバーレイ（名前の変更、複製、削除、表示順の切り替え、エクスポート、インポート）
4. **将来**: 一覧フィルター条件の保存、コピー元のボリュームシリアルによる再解決

### 6.1 コード構成

`MainWindow.xaml.cs` へプリセットの全責務を追加せず、少なくとも次の単位へ分離する。

- `PhotoImporterPreset`: XML へ永続化するプリセットモデル
- `PresetSettingsSnapshot`: 現在設定の取得、適用、検証、正規化後の差分比較
- `PhotoImporterPresetStore`: XML、Mutex、原子的保存、破損退避、インポート・エクスポート
- `PresetManagerViewModel`: 管理オーバーレイの一覧、選択、操作状態
- `PresetApplyState`: 選択中 `id`、未選択時を含む基準値、未保存状態、元に戻すためのスナップショット

日時を使う処理はテストで現在時刻を差し替えられるようにし、`DateTime.UtcNow` をストアやモデルの深部から直接参照しない。

## 7. テスト条件

- §2.1 の全項目が保存・読込を往復して一致する。§2.2 の項目はプリセットへ書き出されず、読み込んでも現在のアプリ全体設定を変更しない
- コピー元を保存していないプリセットの適用で、現在のコピー元が変更されない。保存済みの場合は適用される
- 存在しないフォルダ、不正なテンプレート、不正なサイドカー拡張子を含むプリセットを読み込んでもファイル全体の破損とせず、値が失われない。適用直後に共通検証エラーが表示され、コピー元とコピー先の包含関係違反も検出される
- 不正な現在設定の保存と名前を付けて保存が失敗し、不正な既存プリセットは削除とエクスポートができる。不正なサイドカー拡張子を適用した後も、現在状態を保存しない選択によって終了できる
- サイドカー拡張子が [SIDECAR_SPEC.md](SIDECAR_SPEC.md) の規則で正規化されて保存され、差分判定が正規化後の集合で行われる
- パスの大文字・小文字と末尾区切り文字の差は未保存変更にならず、テンプレートの文字差は未保存変更になる。コピー元を保存していないプリセットではコピー元の差を無視する
- 名前の重複、空文字、前後の空白、制御文字、100文字の上限、100件の上限、上限到達時のエラーを検証する
- リネーム後も `id` による「最後に適用したプリセット」の参照が維持される。参照先が削除済みの場合は未選択として扱う
- 破損ファイルを検出した場合に起動が継続し、可能な場合は退避ファイルが作られ、退避先が既にあっても上書きしない。退避できない場合は元ファイルを変更せず、新しいファイルも作らない
- 未対応の `version` を検出した場合に、元ファイルを変更・退避せず起動を継続する。未知の要素・属性を含む現行版ファイルを再保存しても、それらが維持される
- 2プロセス相当のテストで、片方が追加したプリセットをもう片方の保存が消さない。Mutex のタイムアウトと放棄を検証し、取得後の再読込で対象が削除済みの場合は復活させず失敗する
- 適用がスキャン結果を無効化し、再スキャンを促す。スキャン中・コピー中は適用も保存もできない
- プリセット未選択時の基準値から編集した後にプリセットを選ぶと確認が表示される。名前を付けて保存した後は新規プリセットが選択され、未保存表示が消える
- 「元に戻す」で適用直前の設定、選択 `id`、未保存状態へ戻る。戻した後にファイルと `lastUsedUtc` が変化していない。編集、別プリセット適用、保存、スキャン開始で元に戻す操作が失効する
- 選択中プリセットの削除で現在設定が変わらず、未選択の基準値へ移行する。他プロセスによる削除を一覧再読込で検出した場合も同じ動作になる
- エクスポートしたファイルを別環境でインポートしても、Exif キャッシュの保存先などマシン固有の設定が持ち込まれない。インポートだけでは適用されず、失敗時は部分的な追加・上書きが残らない
- インポートの上書きでローカル側の `id` と `createdUtc` が維持され、別名追加と複製では新しい `id` が発行される。インポート元の `lastUsedUtc` は持ち込まれない
