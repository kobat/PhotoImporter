# レビュー対応計画

作成日: 2026-07-19
最終更新日: 2026-07-25

原典:

- `REVIEW_FINDINGS.md`（初回レビュー）
- `REVIEW_FILTER_FINDINGS.md`（一覧フィルター実装後レビュー）

対象基準:

- 初回レビュー: `main` (`29d497d`)
- 統合時点: `main` (`3b1afbf`, "Complete list filtering with UI and lazy Exif loading")

検証基準:

- 初回レビュー時: Core 単体テスト 115/115 件成功、アプリは別出力先へのビルドで警告 0・エラー 0
- 統合時点: Release 構成のテスト 223/223 件成功 (`dotnet test PhotoImporter.sln -c Release --no-restore`)

## 1. 目的

初回レビューと一覧フィルター実装後レビューで挙げられた指摘について、対応要否、優先度、採用する方針、完了条件を固定し、別の Codex タスクでも同じ判断基準で実装を継続できるようにする。

`REVIEW_FINDINGS.md` と `REVIEW_FILTER_FINDINGS.md` はレビュー原文として扱い、この文書を実装作業の進捗管理と意思決定の正とする。実装中に前提の誤りやより安全な方式が判明した場合は、コード変更より先にこの文書の該当項目へ理由を追記する。

## 2. 優先度

| 優先度 | 意味 |
|---|---|
| P0 | データ破壊や重大なセキュリティ問題。現時点では該当なし |
| P1 | 正式リリース前に対応する。取込漏れ、実環境での恒常的失敗、コピー復旧性に関係する |
| P2 | P1 後に対応する。誤操作、仕様不一致、診断性、運用上の大きな不便に関係する |
| P3 | 通常バックログ。軽微な不具合、性能、文書整合性、保守性の改善 |
| P4 | 条件付き対応または整理候補。現在の要件では実害がなく、必要になった時点で再評価する |

## 3. 共通の完了条件

各タスクは次をすべて満たした時点で完了とする。

- 対象外の指摘や無関係なリファクタリングを混在させない
- 仕様判断を `DESIGN.md`、`TEMPLATE_SPEC.md`、`FILTER_SPEC.md`、`README.md` の適切な箇所へ反映する
- 正常系だけでなく、レビューで指摘された失敗系の自動テストを追加する
- `dotnet test PhotoImporter.sln -c Release --no-restore` が成功する
- `PhotoImporter.App` が警告 0・エラー 0 でビルドできる
- 起動中の `PhotoImporter.exe` が通常出力先をロックしている場合は、プロセスを停止せず別の `OutDir` でビルドする
- 検証用に作成した出力や一時ファイルを残さない
- 本文の状態、実施内容、検証結果、残存リスクを更新する
- 1タスク1コミットを基本とし、コミットメッセージから対象IDを判別できるようにする

## 4. 実施順序

P1-A と P1-B は完了済みである。未完了タスクでは、P1-C、P2-C、P2-D が `MainWindow` のスキャン・再スキャン・例外処理周辺で重なり、P2-E と P2-F がフィルター条件モデル周辺で重なる可能性があるため、これらを並列実行しない。以下の順序を基本とする。

1. [完了] P1-A: コピー先ファイルシステムと取込済判定
2. [完了] P1-B: 読み取り専用ファイルとコピー防衛線テスト
3. [完了] P2-A: パス長とテンプレート事前検証
4. [完了] P2-B: 選択状態と再スキャン
5. [完了] P1-C: 遅延 Exif 適用時の対象集合・コピー計画固定
6. P2-E: 条件編集 UI の状態整合
7. P2-F: Unknown・特殊値・エラー行の仕様確定
8. [完了] P2-C: 対象ファイル列挙とドライブ利用不能時の制御
9. P2-D: 最終例外処理と残存一時ファイル案内
10. P3: 軽微な改善と文書整合
11. P4: 条件付き項目の再評価

---

## 5. 実装タスク

### [x] P1-A: コピー先ファイルシステムと取込済判定

対象: M-2、M-4

#### 採用判断

対応必須。M-4 は異なる写真を無言で「取込済」と判定する可能性があり、明示的なコピー失敗より優先度が高い。M-2 は FAT/FAT32/exFAT のコピー先で更新日時の精度差によりコピー検証や再スキャンが失敗する問題である。

#### 採用方針

- 更新日時比較を一律の Ticks 完全一致や一律 2 秒許容にしない
- コピー先ボリュームのファイルシステムに応じた比較ポリシーを導入する
  - NTFS/ReFS: 原則として完全一致
  - exFAT: 10ms 精度を考慮
  - FAT/FAT32: 2秒精度とローカル時刻保持の特性を考慮
  - 未知のファイルシステム: 安全側に不一致または明示的な非対応として扱う
- 一時ファイルと正式ファイルのコピー後検証にも同じ比較ポリシーを使用する
- `Imported` は少なくとも「サイズ一致」かつ「ファイルシステム精度を考慮した更新日時一致」の場合だけとする
- 現在の `source <= destination` だけによる取込済判定は廃止し、曖昧な場合は `Conflict` または連番候補へ回す
- 同一サイズ・同一時刻の別内容ファイルはサイズと時刻だけでは完全に識別できないことを残存リスクとして明記する
- ハッシュまたはカード識別子を含む取込履歴は、必要性と性能を別途評価し、このタスクへ無断で追加しない

#### 主な対象

- `src/PhotoImporter.Core/Copying/CopyEngine.cs`
- `src/PhotoImporter.Core/Templates/DestinationAllocator.cs`
- `src/PhotoImporter.Core/Metadata/VolumeInfo.cs`
- `src/PhotoImporter.App/MainWindow.xaml.cs`
- 関連するコピー計画・スナップショット型

#### 必須テスト

- 宛先サイズが異なる場合は、宛先時刻が新しくても `Imported` にならない
- 宛先サイズと時刻が一致する場合だけ `Imported` になる
- 宛先時刻が新しいだけでは `Imported` にならない
- NTFS 相当では許容範囲外の時刻差を拒否する
- exFAT 相当の 10ms 精度を考慮してコピー後検証できる
- FAT/FAT32 相当の 2秒精度を考慮してコピー後検証できる
- 未知のファイルシステムの扱いが仕様どおりである
- 再スキャン時に正常コピー済みファイルが再び上書き対象や競合にならない

#### 完了記録

- 実施内容: コピー先ボリュームから NTFS/ReFS・exFAT・FAT/FAT32 の更新日時比較ポリシーを決定し、取込済判定、コピー先再検証、一時ファイル検証、正式ファイル検証で共用するようにした。`Imported` はサイズとポリシー適用後の更新日時が両方一致する場合だけとし、それ以外は競合または連番候補へ回す。未知のファイルシステムはスキャン時に明示的な非対応エラーとする。`DESIGN.md`、`TEMPLATE_SPEC.md`、`README.md` も同じ仕様へ更新した。
- 検証結果: `dotnet test PhotoImporter.sln --no-restore` は 127 件成功。起動中アプリが通常出力先をロックしていたため、別 `OutDir` で `PhotoImporter.App` をビルドし、警告 0・エラー 0 を確認した。`git diff --check` も成功した。検証用 `OutDir` は削除済み。
- コミット: 未実施（コミット作成の依頼なし）。
- 残存リスク: 同一サイズ・同一更新日時の別内容ファイルは識別できない。ハッシュまたはカード識別子を含む取込履歴は別途評価が必要。FAT/FAT32・exFAT の実媒体でのコピーは未検証。

### [x] P1-B: 読み取り専用ファイルとコピー防衛線テスト

対象: M-1、T-1

#### 採用判断

対応必須。DCF プロテクト写真は実データで発生し、失敗時の `.partial` 残留と読み取り専用宛先の上書き不能を引き起こす。

#### 採用方針

- 既存の多層安全条件をすべて満たした、自アプリが作成した `.partial` だけを属性変更の対象とする
- 安全な `.partial` の削除直前に ReadOnly 属性だけを解除し、削除する
- 正式な宛先の読み取り専用属性は、最終再検証後かつ確定直前に限って解除する
- `MoveFileExW` が失敗し、旧宛先が残っている場合は読み取り専用属性の復元を試みる
- 属性復元を確認できない場合は通常エラーに丸めず、復旧が必要な状態として利用者へパスを示す
- コピー成功後はコピー元から引き継がれた属性を保持する
- 既存の一時ファイル削除条件、宛先再検証、TOCTOU 防御を緩めない

#### 主な対象

- `src/PhotoImporter.Core/Copying/CopyEngine.cs`
- `src/PhotoImporter.Core/Copying/CopyFile2Native.cs`
- `tests/PhotoImporter.Core.Tests/CopyEngineTests.cs`

#### 必須テスト

- 読み取り専用ソースのコピーに成功し、正式ファイルが期待する属性を持つ
- キャンセル時に読み取り専用 `.partial` が削除される
- リネーム失敗時に、安全条件を満たす読み取り専用 `.partial` が削除される
- 安全条件を満たさないファイルの属性を変更・削除しない
- 読み取り専用宛先の上書きに成功する
- 上書き失敗時に旧宛先の属性が復元される
- 属性復元を確認できない場合に復旧パスを含むエラーになる
- スキャン後の宛先出現を検出して非上書きコピーを中止する
- スキャン後の宛先変更を検出して上書きコピーを中止する
- `MoveFileExW` 失敗時の削除と保全が既存方針どおりに分岐する

#### 完了記録

- 実施内容: 安全条件を満たす現在処理中の `.partial` に限り、削除直前に ReadOnly 属性だけを解除するよう変更した。上書き先は最終再検証後かつ `MoveFileExW` 直前に ReadOnly を解除し、確定失敗時は旧宛先のスナップショット一致を確認して ReadOnly を復元・再確認する。復元または一時ファイル削除を確認できない場合は `CopyRecoveryException` として一時ファイルを保全し、旧宛先と一時ファイルの両パスを利用者へ示す。コピー・確定・属性操作にテスト用の内部境界を設け、読み取り専用コピー、キャンセル、確定失敗、属性復元失敗、宛先の途中出現・変更、安全条件不一致、一時ファイル削除・保全分岐を追加した。`DESIGN.md` と `README.md` も同じ仕様へ更新した。
- 検証結果: `dotnet test PhotoImporter.sln --no-restore -c Release` は 137 件成功。`dotnet build PhotoImporter.sln --no-restore -c Release` は警告 0・エラー 0。`git diff --check` も成功した。読み取り専用ソースと読み取り専用上書き先は実ファイル属性で検証し、`MoveFileExW` 失敗と属性復元失敗は決定的なテスト代替で検証した。
- コミット: 未実施（この作業ツリーに実装済み）
- 残存リスク: 最終再検証から ReadOnly 解除・`MoveFileExW` までの上書き TOCTOU 窓は Windows API 上原子的に閉じられず、D-7 の既知リスクとして残る。属性復元不能はテスト代替で検証しており、実デバイス固有の ACL・ウイルス対策ソフト・故障媒体による失敗は未検証。

### [x] P1-C: 遅延 Exif 適用時の対象集合・コピー計画固定

対象: F-1、FT-2

#### 採用判断

正式リリース前に対応必須。Exif 条件の「適用」が全再スキャンを行うため、利用者が手動スキャンで確認していない新規ファイルを自動選択し、既存の選択行についてもコピー先、`{Sequence}`、コピー元・コピー先スナップショットを差し替え得る。フィルター操作がコピー計画を暗黙に変更することは、コピー対象を利用者が確認するという最終防衛線に反する。

#### 採用方針

- 遅延 Exif 読込では、手動スキャンで確定した対象集合、行順、選択状態、`CopyPlan`、コピー先、`{Sequence}`、ファイルスナップショットを変更しない
- 一覧全体を `BuildPreview` で再構築する方式をやめ、既存行に必要な Exif 結果だけを後追いで関連付けるか、同じコピー計画を保持する新しい行へ置換する
- Exif 読込対象は既存一覧のパスと RAW+JPEG 解析元に限定し、処理中にコピー元へ追加されたファイルを一覧へ取り込まない
- 既存ファイルの削除・変更、解析元の変更などにより安全に関連付けできない場合は、新しいフィルターを適用せず、直前の一覧・条件・選択・コピー計画を維持して手動再スキャンを促す
- 新条件は、必要な Exif 読込と全件のフィルター評価が成功した後に一度だけ反映する
- 一覧の置換が必要な場合も、直前の適用済みフィルターで `Refresh` してから新条件を適用する順序を禁止する。非表示チェックの解除は新条件に対して一度だけ行う
- キャンセル、全体エラー、正規表現タイムアウトでは作成途中の結果へ切り替えない
- 「新規行だけを未選択にする」対応だけでは、既存行のコピー計画変更を防げないため採用しない

#### 主な対象

- `src/PhotoImporter.App/MainWindow.xaml.cs`
- `src/PhotoImporter.App/PreviewItemCollectionState.cs`
- 遅延 Exif 読込と既存行への結果関連付けを担う新しい純粋ロジックまたはサービス
- `tests/PhotoImporter.Core.Tests/PreviewSelectionStateTests.cs`
- 新しい遅延 Exif 適用テスト

#### 必須テスト

- 遅延 Exif 読込中にコピー元へ新しいファイルが追加されても、一覧とコピー対象へ追加されない
- 遅延 Exif 読込前後で既存行の `CopyPlan`、コピー先、`{Sequence}`、コピー元・コピー先スナップショットが同一である
- チェック済み・未チェックの状態が全既存行で維持される。ただし新フィルターによる非表示チェック解除は設定どおりに一度だけ行われる
- 旧フィルターでは非表示、新フィルターでは表示となる行のチェックが、旧フィルターの再評価によって外れない
- 既存ファイルまたは RAW+JPEG 解析元が処理中に変更・消失した場合、新条件を適用せず直前状態を維持する
- キャンセル、Exif 読取全体エラー、フィルター評価例外で直前の一覧・条件・選択・コピー計画を維持する
- Exif キャッシュ利用有無と RAW+JPEG 共有規則が通常スキャンと一致する

#### 完了記録

- 実施内容: Exif 条件適用時の `BuildPreview` による全再スキャンを廃止し、手動スキャン済みの行とパス集合から `LazyExifPreviewLoadPlan` を作る方式へ変更した。固定した集合だけで通常スキャンと同じ `RawJpegAnalysisPlan` と `CachedPhotoMetadataScanner` を使用し、読込前後に全対象のサイズ・更新日時をスキャン時値と照合する。新規ファイルは取り込まず、全件の新フィルター評価と一覧同一性確認が成功した後だけ、既存行へ Exif 結果と解析元時刻を関連付けるため、行順、選択、`CopyPlan`、コピー先、`{Sequence}`、コピー元・コピー先スナップショットは不変となる。旧フィルターでの中間 `Refresh` を除去し、新条件に対して表示外チェック方針を一度だけ適用するようにした。`FILTER_SPEC.md`、`DESIGN.md`、`README.md` も同じ仕様へ更新した。
- 検証結果: 新規ファイル追加、コピー計画と選択状態の同一性、RAW+JPEG 解析元共有、読込前変更、読込中変更、キャンセル、Exif 読取全体エラー、フィルター評価例外、一覧差し替えを対象とする遅延 Exif テストを追加した。`dotnet test PhotoImporter.sln --no-restore -c Release` は 231 件成功。`dotnet build PhotoImporter.sln --no-restore -c Release` は警告 0・エラー 0。`git diff --check` も成功した。
- コミット: 未実施（コミット作成の依頼なし）。
- 残存リスク: 最終スナップショット照合後から Exif 結果の UI 反映までの短い間に外部プロセスがファイルを変更する TOCTOU 窓は残る。ただしコピー時には保持済みのコピー元スナップショットを再検証するため、変更後ファイルがそのままコピーされることはない。サイズと更新日時が同一になるよう内容だけを差し替える変更は検知できず、これは P1-A と同じ残存リスクである。実機写真、実カード、実キャッシュを使った UI 操作は未検証。

### [x] P2-A: パス長とテンプレート事前検証

対象: M-3、M-5

#### 採用判断

対応する。どちらも安全側に失敗するが、本来は入力時に1回だけ示すべき問題がファイル単位のスキャンエラーとして表面化している。

#### 採用方針

- `DestinationAllocator` から `TemplateEvaluator.EvaluateDetailed` へ `destinationRoot` と決定した上限を渡す
- 長いパスをサポートする範囲と上限値を仕様で決定し、コードの既定値 259 と矛盾させない
- .NET Framework 4.8.1 に必要な `app.config` のパス処理設定を追加する
- Per-Monitor V2 DPI と `longPathAware` を含む manifest を追加し、プロジェクトへ接続する
- 日付書式をサンプル日時でパース時に検証する
- 日付書式の評価結果にパス不正文字が出るテンプレートを入力エラーとして扱う
- 先頭・末尾区切り、空パス要素など静的に判定できる構造エラーをパース時に返す
- ファイル固有値によってのみ発生するパスエラーは、引き続き行単位エラーとして扱う

#### 必須テスト

- 宛先ルート込みの上限直前・上限超過
- 長いルートと短い相対パスの組み合わせ
- `{ModifiedDate:HH:mm}` を入力時に拒否する
- 不正な日時書式を `InvalidDateFormat` として返す
- 先頭・末尾区切りと空要素を入力時に拒否する
- 正常な日時書式と複数階層テンプレートを誤拒否しない
- manifest/config を含むアプリビルドが成功する

#### 完了記録

- 実施内容: 宛先ルートと32,767文字上限を `DestinationAllocator` から全テンプレート評価へ渡し、ルート込みのフルパス長を検証するようにした。日時書式をサンプル日時でパース時に評価し、不正書式およびパス不正文字を生成する書式を `InvalidDateFormat` とした。先頭・末尾・連続区切り、静的な空要素、`.`、`..`、空の `{Sequence}` 要素を `InvalidPathStructure` として入力時に拒否する。長いパスと Per-Monitor V2 を有効にする App.config / manifest を追加してアプリへ接続し、DESIGN / TEMPLATE_SPEC に上限と前提を明記した。
- 検証結果: `dotnet test PhotoImporter.sln --no-restore -c Release` は146件成功。`dotnet build PhotoImporter.sln --no-restore -c Release` は警告0・エラー0。生成された `PhotoImporter.exe.config` に長いパス用 AppContext スイッチが含まれること、および `git diff --check` の成功を確認した。
- コミット: 未実施（この作業ツリーに実装済み）
- 残存リスク: 260文字超の実利用には Windows 側の LongPathsEnabled が必要であり、無効な端末ではOS/API側のエラーになる。ファイル固有の値でのみ発生する予約名、不正要素、各ファイルシステム固有の要素長制限は従来どおり行単位エラーとして扱う。

### [x] P2-B: 選択状態と再スキャン

対象: M-7、T-2、D-9

#### 採用判断

対応する。コピー対象抽出は安全だが、チェックボックス表示とモデルが食い違い、再スキャン後に利用者が外した対象が再選択される。

#### 採用方針

- `DataGridCheckBoxColumn` の表示要素を `CanCopy` に連動して無効化する
- `IsSelected` が要求値を拒否した場合も、表示をモデルへ戻す通知を行う
- コピー後の自動再スキャンでは、コピー元相対パスをキーとして未処理行の選択状態を引き継ぐ
- コピーに成功して `Imported` になった行、競合行、エラー行は常に選択解除する
- 手動の新規スキャンで状態を引き継ぐかは仕様で明示し、コピー後再スキャンと混同しない

#### 必須テスト

- `CanCopy == false` の行へ `IsSelected = true` を設定しても false のままで通知される
- `SetCopyError` 後に選択解除され、再選択できない
- コピー可能行だけが初期選択される
- コピー後再スキャンで利用者が外した未処理行は外れたままになる
- コピー成功・競合・エラー行は再スキャン後も選択されない
- 無効行のチェックボックスが操作不能として表示される

#### 完了記録

- 実施内容: 対象列の表示・編集用チェックボックスを `CanCopy` に連動して無効化した。拒否された `IsSelected` 要求でも通知して表示をモデルへ戻し、コピーエラーを `CanCopy == false` として再選択不能にした。コピー開始前の選択状態をコピー元相対パスで記録し、自動再スキャン後に未処理行へ復元する一方、コピー成功・競合・スキャンエラー・コピーエラー行は選択解除するようにした。手動スキャンは従来どおりコピー可能行を初期選択する仕様として DESIGN / README に明記した。大量行の選択復元中は行ごとのサマリー再集計を抑止し、完了後に1回だけ更新するようにした。
- 検証結果: `dotnet test PhotoImporter.sln --no-restore -c Release` は156件成功。`dotnet build PhotoImporter.sln --no-restore -c Release` は警告0・エラー0。`git diff --check` も成功した。対象列ヘッダーの全選択・一部選択・全解除状態と、コピー不可行を一括変更から除外するテストを含む。
- コミット: 未実施（この作業ツリーに実装済み）
- 残存リスク: 無効チェックボックスのデータバインドと表示・編集スタイルはXAMLコンパイルで確認したが、実画面でのマウス・キーボード操作と高コントラスト表示は手動確認していない。

### [x] P2-E: 条件編集 UI の状態整合

対象: F-2、F-3、F-4、FT-1

#### 採用判断

対応する。項目切替後に画面表示と実際の条件が食い違う、または非表示になった専用オプションによって条件行が回復不能な無効状態になる問題である。`{Extension}` に仕様上存在しない大小文字区別を許す問題も同じ条件編集モデルと XAML にあるため、一つのタスクで整合させる。

#### 採用方針

- `SelectedField` 切替時に `IncludeUnknown`、`IncludeNoSequence`、`IncludeRejectedRating` を原子的に既定値へ戻す
- リセットした全プロパティについて `PropertyChanged` を発火し、チェックボックス表示と条件モデルを一致させる
- 中間状態ごとに重複検証を走らせず、必要な状態変更後に検証通知をまとめて行う
- `{Sequence}` と `{Rating}` の専用オプションは対応フィールド以外の条件へ持ち越さない
- `OptionNotSupported` は防御的検証として残し、到達した場合も原因が分かる行内メッセージへ変換する
- `{Extension}` は常に大文字・小文字を区別せず比較する。大小文字区別チェックボックスを非表示または無効にするだけでなく、条件構築時にも `CaseSensitive = false` を保証する
- F-4 の正確な問題は「大小文字区別 ON かつ大文字を含む入力が、常に小文字へ正規化された候補へ一致しない」であり、小文字入力まで常に不一致になるわけではないことを記録する

#### 主な対象

- `src/PhotoImporter.App/FilterUiModels.cs`
- `src/PhotoImporter.App/MainWindow.xaml`
- `tests/PhotoImporter.Core.Tests/FilterUiModelTests.cs`

#### 必須テスト

- 項目切替後に `IncludeUnknown`、`IncludeNoSequence`、`IncludeRejectedRating` が false になり、各 `PropertyChanged` が発火する
- `{Sequence}` で「連番なし」を選択してから他の数値項目へ切り替えても条件行が有効である
- `{Rating}` で `Rejected` を選択してから他の数値項目へ切り替えても条件行が有効である
- 項目切替後のチェックボックス表示と `TryBuild` が使用する値が一致する
- `{Extension}` では大小文字区別を有効化できず、`.ARW` と `.arw` が同じ結果になる
- 他の文字列フィールドでは従来どおり大小文字区別を選択できる
- `OptionNotSupported` が発生した場合に具体的な行内理由が表示される

#### 完了記録

- 実施内容: `SelectedField` 切替時に `IncludeUnknown`、`IncludeNoSequence`、`IncludeRejectedRating` をバッキングフィールド上で一括して `false` へ戻し、変更された各プロパティの通知後に検証通知を1回へまとめるよう修正した。`{Extension}` へ切り替えた場合は `CaseSensitive` も解除して通知し、WPF の大小文字区別チェックボックスを無効化した。さらに `CaseSensitive` セッターと `TryBuild` の両方で `{Extension}` の大小文字区別を拒否し、`OptionNotSupported` を `{Sequence}` / `{Rating}` の専用オプションが原因と分かる行内メッセージへ変換した。項目切替、通知回数、Sequence / Rating から Iso への切替、拡張子の大小文字非区別、通常文字列項目の大小文字区別、専用オプションの診断を自動テストへ追加した。
- 検証結果: `dotnet test PhotoImporter.sln -c Release --no-restore` は 237 件成功。`dotnet build PhotoImporter.sln -c Release --no-restore` は警告 0・エラー 0。`git diff --check` も成功。
- コミット: 未実施（この作業ツリーに実装済み）。
- 残存リスク: WPF 画面上での実クリックによるチェック状態・無効表示の確認は未実施。モデルと XAML バインディング、および条件評価結果は自動テストと Release ビルドで確認済み。

### [x] P2-F: Unknown・特殊値・エラー行の仕様確定

対象: F-5、FD-1、FD-2、FT-3

#### 採用判断

仕様決定後に対応する。現状は「通常行では必ず値があるため Unknown を選択できない」という定義と、「スキャンエラーや読取不能では値を取得できず Unknown になり得る」という定義が両立していない。また、対象から外す条件と `{Sequence}` の「連番なし」・`{Rating}` の `Rejected` の組合せを仕様とテストで固定する必要がある。

#### 採用方針

- `{Extension}` と `{Protected}` は通常行では既知値だが、スキャンエラー行では Unknown になり得るフィールドとして扱い、`IncludeUnknown` を指定可能にする
- Unknown の既定は従来どおり「除外する」とし、スキャンエラー行も表示したい条件では利用者が「Unknownを含める」を指定できるようにする
- `{HasGps}` は読取結果ごとの意味を仕様へ明記する
  - 正常読込で GPS 値あり: `GPS`
  - 正常読込で GPS 値なし、または `NoMetadata`: `NoGPS`
  - `Unsupported`、`ReadError`、スキャンエラー: GPS の有無を判定できないため Unknown
- 上記に合わせて `{HasGps}` の `CanBeUnknown`、候補値生成、UI、テストを統一する
- 「対象から外す」は、数値範囲と同一条件内で選択した特殊値を合わせた既知値マッチ結果全体を反転する
  - 「評価1～5を対象から外す」で `Rejected` を選択していなければ、`Rejected` は対象に残る
  - 「連番範囲を対象から外す」で「連番なし」を選択していなければ、「連番なし」は対象に残る
- Unknown は既知値マッチの反転と独立し、`FILTER_SPEC.md` §4.2 の表どおり `IncludeUnknown` だけで決定する
- 特殊値の対象・除外表と Exif 読取状態別 `{HasGps}` の表を `FILTER_SPEC.md` に追加する

#### 主な対象

- `FILTER_SPEC.md`
- `src/PhotoImporter.Core/Filtering/FilterModels.cs`
- `src/PhotoImporter.Core/Filtering/FilterCandidate.cs`
- `src/PhotoImporter.Core/Filtering/FilterEvaluator.cs`
- `src/PhotoImporter.App/FilterUiModels.cs`
- `tests/PhotoImporter.Core.Tests/FilterEvaluatorTests.cs`
- `tests/PhotoImporter.Core.Tests/FilterUiModelTests.cs`
- `tests/PhotoImporter.Core.Tests/PreviewSelectionStateTests.cs`

#### 必須テスト

- スキャンエラー行の `{Extension}` と `{Protected}` が Unknown になり、`IncludeUnknown` の ON/OFF で表示結果が変わる
- 通常行の `{Extension}` と `{Protected}` は既知値として従来どおり評価される
- `{HasGps}` が正常読込、GPS欠落、`NoMetadata`、`Unsupported`、`ReadError`、スキャンエラーで仕様表どおりになる
- `{HasGps}` の Unknown を UI から含める・除外する条件を構築できる
- `{Sequence}` の数値範囲、「連番なし」、対象・除外の全組合せ
- `{Rating}` の数値範囲、`Rejected`、対象・除外の全組合せ
- 特殊値の選択有無にかかわらず Unknown の判定が独立している

#### 完了記録

- 実施内容: `FILTER_SPEC.md` に Exif 読取状態別の `{HasGps}` 判定表と、`{Sequence}` の「連番なし」・`{Rating}` の `Rejected` に対する対象／除外表を追加した。`{Extension}`、`{Protected}`、`{HasGps}` を Unknown 指定可能なフィールドへ変更し、スキャンエラーの拡張子・読み取り専用状態を Unknown として評価するよう統一した。`{HasGps}` は正常読込の GPS 有無を `GPS` / `NoGPS`、`NoMetadata` を `NoGPS`、`Unsupported`・`ReadError`・スキャンエラーを Unknown として返すよう修正した。特殊値を含む既知値マッチ全体の反転と Unknown の独立判定は既存実装が採用方針どおりだったため変更せず、全組合せテストで仕様を固定した。UI モデルで3フィールドの「Unknownを含める」が有効になり、`{HasGps}` の Unknown を含める／除外する条件を構築できることもテストへ追加した。
- 検証結果: `dotnet test PhotoImporter.sln -c Release --no-restore` は 274 件成功。`dotnet build PhotoImporter.sln -c Release --no-restore` は警告 0・エラー 0。`git diff --check` も成功。
- コミット: 未実施（この作業ツリーに実装済み）。
- 残存リスク: WPF 画面上での実クリックによる「Unknownを含める」の有効表示確認は未実施。条件モデル、候補値、プレビューのスキャンエラー行、および評価結果は自動テストと Release ビルドで確認済み。

### [x] P2-C: 対象ファイル列挙とドライブ利用不能時の制御

対象: D-2、D-5

#### 採用判断

対応する。写真取込アプリとして対象外ファイルや既知のシステム領域を取り込まず、ドライブ全体が利用不能になった場合は同じエラーをファイル・フォルダごとに繰り返さない。

#### 採用方針

- 対応拡張子を単一のレジストリまたは判定関数に集約する
- 列挙、RAW+JPEG 判定、Exif 対応可否の拡張子定義を可能な限り共有する
- `Thumbs.db`、`desktop.ini` と既知のシステム領域を仕様に基づいて除外する
- 個別フォルダのアクセス拒否は既存どおり継続可能とする
- コピー元ルートまたはコピー先ルート自体が利用不能になった場合はバッチを中止し、単一の明確なエラーとして表示する
- ドライブ脱着を誤判定しないよう、個別ファイルエラーとの判定条件をテスト可能な形に分離する

#### 必須テスト

- 対応拡張子だけがプレビュー対象になる
- 大文字小文字の異なる拡張子を同じように扱う
- `Thumbs.db`、`desktop.ini`、既知のシステム領域を除外する
- 個別サブフォルダのアクセス拒否ではスキャンを継続する
- コピー元ルート利用不能ではスキャン全体を中止する
- コピー先ルート利用不能では残り全件を個別失敗にせずコピー全体を中止する

#### 完了記録

- 実施内容: `PhotoFileClassifier` を対応拡張子の単一レジストリとし、列挙、ファイル分類、RAW+JPEG判定で共有した。`SourceFileEnumerator` を抽出し、`Thumbs.db`、`desktop.ini`、`System Volume Information`、`$RECYCLE.BIN`、`RECYCLER`、`RECYCLED` を除外した。個別サブフォルダーのアクセス拒否はエラー行として継続し、コピー元ルート喪失は単一例外でスキャンを中止する。コピーでは各項目の開始前とファイルエラー後にコピー元・コピー先ルートを確認し、ルート利用不能時は `CopyBatchResult.BatchError` に単一の明確なエラーを設定して残件を処理せず、UI も再スキャンせずそのエラーを表示するようにした。`DESIGN.md` と `README.md` を同じ仕様へ更新した。
- 検証結果: `dotnet test PhotoImporter.sln -c Release --no-restore` は 285 件成功。対応拡張子の大小文字、対象外ファイル・システム領域、個別サブフォルダーのアクセス拒否継続、コピー元ルート利用不能によるスキャン中止、コピー元・コピー先ルート利用不能によるコピー全体中止を自動テストで確認した。Release テスト時にアプリも警告 0・エラー 0 でビルドされた。
- コミット: 未実施（コミット作成の依頼なし）。
- 残存リスク: 実カードの物理抜去タイミングとネットワーク共有切断は実機未検証。ルート利用可能性は `Directory.Exists` で判定するため、OS が一時的に利用不能を不存在として返す障害は安全側にバッチ中止となる。

### [ ] P2-D: 最終例外処理と残存一時ファイル案内

対象: M-6、D-11、F-7

#### 採用判断

対応する。ただし主要な `Scan_Click` と `Copy_Click` には既に例外処理があるため、グローバルハンドラは通常制御ではなく最後の防衛線とする。コピー後の選択復元に伴うフィルター再評価は `Copy_Click` の既存 `try/catch` の外にあり、正規表現タイムアウトなどを未処理例外として UI スレッドへ漏らし得るため、局所的な例外処理も追加する。

#### 採用方針

- WPF の未処理例外を記録し、利用者へエラーダイアログを表示する
- 未知の例外を握りつぶして処理継続せず、安全な終了を基本とする
- `TaskScheduler.UnobservedTaskException` は診断記録を目的とし、通常のエラー伝播の代替にしない
- コピー後再スキャン・選択復元・`ICollectionView.Refresh` は `Copy_Click` の局所的な `try/catch` で保護し、`FilterEvaluationException` を通常のフィルター評価エラーとして表示する
- 局所処理で回復できるフィルター評価例外をグローバルハンドラへ送らない
- コピー自体が完了した後の表示更新に失敗した場合は、コピー結果を失敗と誤表示せず、コピー完了と一覧更新失敗を分けて案内し、手動再スキャンを促す
- 設定保存は閉じる時だけに依存せず、重要な設定確定時の保存を P3 の L-6 と連携して検討する
- 起動時にコピー先候補配下の厳密な命名規則に一致する `.partial` を検出する
- 検出時は自動的に成功・失敗を断定せず、正式ファイルとの状態、保存場所、利用者が取るべき操作を案内する
- 自動削除を追加する場合は既存の多層安全条件と同等以上の条件を要求する

#### 必須テスト

- UI スレッドの未処理例外が記録・表示され、安全な終了へ進む
- 通常のスキャン・コピー例外がグローバルハンドラへ漏れない
- コピー後のフィルター再評価で `FilterEvaluationException` が発生しても未処理例外にならず、コピー結果と一覧更新エラーが区別して表示される
- コピー後の選択復元と表示更新が成功する通常経路を壊さない
- 命名規則に一致する `.partial` だけを検出する
- リパースポイント、ディレクトリ、ルート外、名前不一致を復旧候補にしない
- 正式ファイルの有無とスナップショット状態に応じて案内を変える

#### 完了記録

- 実施内容:
- 検証結果:
- コミット:
- 残存リスク:

---

## 6. P3 バックログ

以下はP1/P2完了後に、相互に独立した小さなコミットへ分けて対応する。

| 状態 | ID | 対応方針 |
|---|---|---|
| [ ] | L-1 | ペア解析元 `FileInfo` の `Exists` と再スナップショットを確認し、1601年フォールバックを防ぐ |
| [ ] | L-2 | 書込み確認と利用者確認の順序を整理し、キャンセル時に新規空フォルダを残さない |
| [ ] | L-4 | 実在しない互換経路を削除するか、旧JSON破棄という実際の仕様へ文書を統一する |
| [ ] | L-6 | 重要な設定変更の確定時保存を追加し、終了時保存だけに依存しない |
| [ ] | L-8 | 進捗を「コピー成功バイト」か「処理済みバイト」か定義し、失敗時も終了表示が一貫するようにする |
| [ ] | L-9 | コピー後再スキャンで変更のないファイルのExif再読取を避ける方法を性能計測後に選ぶ |
| [ ] | L-10 | READMEのテスト件数、既定テンプレートの重複、Invalid/Missingオフセット警告を整理する |
| [ ] | D-1 | 既定テンプレートを実装値へ統一し、採用理由を仕様へ反映する |
| [ ] | D-3 | `IFileOpenDialog` へ置換するか、現実装を許容するなら設計記述を修正する |
| [ ] | D-4 | 3文字コードが地域タイムゾーンでDSTを含むこと、`CST` 等の地域を明記する |
| [ ] | D-8 | コピー可能な行とチェック不可の行を仕様に明記する |
| [ ] | D-10 | 保存時と候補表示でトークン名を正式表記へ正規化する |
| [ ] | F-8 | 条件編集の入力遅延を計測し、必要な場合だけ `IsValid` と `ValidationMessage` が共有する検証結果をキャッシュする。準備済み正規表現は既に項目評価ごとに再構築されないため、直ちに必須の仕様違反とは扱わない |
| [ ] | F-10 | `PreviewItemCollectionState.Refresh` でフィルター未適用時の非表示項目探索を省き、不要な `HashSet` 構築を避ける |
| [ ] | FD-3 | フィルター条件と非表示チェック解除設定の永続化は v1 対象外・v2 候補であることを `DESIGN.md` と `FILTER_SPEC.md` で相互参照する |

## 7. P4 条件付き・見送り候補

| 状態 | ID | 判断 |
|---|---|---|
| [ ] | L-3 | `ExifCacheKeyPlan` は未使用。並列化で使う予定を明文化するか、その計画がなければ削除する |
| [ ] | L-5 | セッションをまたぐ共有キャッシュが要件になった場合だけ `Global\` Mutex と権限設計を検討する |
| [ ] | L-7 | 現状も無効ハンドルなら `FlushFileBuffers` が失敗して安全側に中止する。診断メッセージ改善以上の実害はない |
| [ ] | D-6 | `SequenceExhausted` の表示分類だけの問題。文言または仕様を統一する |
| [ ] | D-7 | 解消不能な上書き確定直前のTOCTOUを既知の残余リスクとして設計へ記載する |
| [ ] | F-6 | スキャンエラーを Exif 読取エラーとして扱う現挙動はテストで固定済みで、`Unread` も遅延読込前の内部状態として使用される。両者を UI で分離する要件が生じた場合だけ再評価する |
| [ ] | F-9 | `RequiresExif == true` なら準備済み条件は非nullかつ条件数1以上であり、現在の経路で null 参照へ到達しない。周辺をリファクタリングする場合は `_appliedFilter` ではなく引数 `prepared` を捕捉して不変条件を明確化する |

## 8. 新しい Codex タスクへ渡す指示

各実装タスクでは次の形式を使用する。

```text
REVIEW_ACTION_PLAN.md の「<タスクIDと名称>」を実装してください。
REVIEW_FINDINGS.md と REVIEW_FILTER_FINDINGS.md はレビュー原典として参照してください。

要件:
- REVIEW_ACTION_PLAN.md の採用方針と対象範囲を守る
- 範囲外の指摘には手を付けない
- 作業前に既存実装、仕様、テストを確認する
- 必須テストを追加する
- dotnet test とアプリのビルドを実行する
- 完了後に REVIEW_ACTION_PLAN.md の状態、実施内容、検証結果、コミット、残存リスクを更新する
- 変更内容、残存リスク、テスト結果を報告する
```

推奨ブランチ名:

- `codex/review-p1-destination-identity`
- `codex/review-p1-readonly-copy`
- `codex/review-p2-template-validation`
- `codex/review-p2-selection-state`
- `codex/review-p1-filter-plan-stability`
- `codex/review-p2-filter-editor-state`
- `codex/review-p2-filter-semantics`
- `codex/review-p2-scan-resilience`
- `codex/review-p2-recovery-ui`
