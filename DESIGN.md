# 写真取込アプリ 検討メモ

作成日: 2026-07-12
最終更新日: 2026-07-14
状態: 開発ステップ5まで実装済み。Exif系を含むテンプレートを使ったスキャン、判定、プレビュー、コピー実行が利用可能。実機写真による形式別検証は継続中。技術スタックは **.NET Framework 4.8.1 + WPF**。

## 1. 目的・要件

Windows 用デスクトップアプリ。カメラ・SDカード・CFexpress カードから写真をPCへコピーして取り込む。

- コピー先のフォルダ・ファイル名を**テンプレート**で柔軟に設定できる
  - 例: `{TakenDate:yyyy-MM-dd}\{OriginalName}`
- コピー元とコピー先を比較し、**未取込の写真のみ**を判断してコピーする
- コピー元フォルダの直下だけでなく、**すべてのサブフォルダを再帰的に走査**する。デジタルカメラが `DCIM` 配下に複数の保存フォルダを作成する場合や、保存フォルダが複数階層になる場合も対象とする
- 使い方の流れ:
  1. 起動後、コピー元フォルダ・コピー先フォルダを選択し、テンプレートを入力
  2. 未取込の写真を一覧表示し、利用者が確認
  3. 未取込の写真をコピー
- 画面では左にコピー元ファイル、右にコピー先(予定)ファイルを並べ、どうコピーされるか視覚的に分かるようにする
- 未取込のみ/コピー済も含む全件、の表示切替ができる
- UI は**日本語・英語の2言語対応**とする。テンプレートのトークン名は表示言語によらず英語で統一する

### 配布要件

- **配布は exe のみ(単一ファイル)**
- ターゲットは Windows 11 に限定。.NET Framework 4.8.1 が OS 標準搭載のため、ランタイム同梱不要で exe を小さくできる
- WinUI 3 + Self-contained + Single-file は exe が数百MBになるため不採用
- Exif キャッシュの既定保存先は実行ファイルと同じ場所に置き、利用者がエクスプローラーで存在と容量を確認できるポータブル寄りの構成とする。実行ファイルのフォルダが書込み禁止の場合も、ユーザーから見えにくい別フォルダへ自動退避しない

## 2. 技術スタック(決定済み)

| 項目 | 選定 | 理由・備考 |
|---|---|---|
| フレームワーク | .NET Framework 4.8.1 + **WPF** | Windows 11 標準のランタイムを利用でき、exe が小さい。WinForms より XAML レイアウト・DPI対応・バインディングが快適 |
| csproj | SDK スタイル + `<TargetFramework>net481</TargetFramework>` | `dotnet build` 可、NuGet は PackageReference |
| Exif 読み取り | **MetadataExtractor**(NuGet) | ファイル形式に応じたメタデータ領域を読む。netstandard2.0 を介して net481 で動作。JPEG/RAW/動画ごとの読込量と速度は実データで検証する |
| Exif キャッシュ | **設定可能なキャッシュルート + ボリュームシリアル番号ごとのフォルダ + JSON ファイル**(起動時はメタのみ読み、エントリはスキャン対象ボリューム分を遅延読み込み) | 既定ルートは実行ファイル直下の `ExifCache`。SQLite はネイティブDLLを伴い単一exe化と相性が悪いので不採用。件数が増えて困れば LiteDB(純マネージド)に切替。詳細は §4 |
| 単一 exe 化 | **Costura.Fody**(依存DLLをリソース埋め込み)または ILRepack | マネージドDLLのみなら安定 |
| フォルダ選択 | `IFileOpenDialog` の COM 相互運用ラッパー(50行程度) | net481 標準の FolderBrowserDialog は古いツリー型のため |
| 構成 | コアロジック(テンプレート解析・差分判定・キャッシュ)は **UI と分離したクラスライブラリ** + 単体テスト(xUnit等) | 一番バグりやすく一番テストしやすい部分 |
| UI 多言語化 | `.resx` リソース(`ja-JP` / `en-US`) | UI文言とエラーメッセージをローカライズ。テンプレート構文と保存値は言語非依存 |

### net481 の既知の制約と対処

- **C# 言語バージョン**: 公式は C# 7.3。`LangVersion` を上げれば record / init も `IsExternalInit` ポリフィルで使用可。`Parallel.ForEachAsync` は無いので `SemaphoreSlim` + `Task.WhenAll` で並列化。`System.Text.Json` は NuGet 版が net481 で動く
- **High DPI**: Per-Monitor V2 は app.manifest / app.config での明示設定が必要
- **長いパス(260文字超)**: app.config の `Switch.System.IO.UseLegacyPathHandling=false` + OS 側 LongPathsEnabled。加えてアプリ側で宛先パス長のバリデーションを入れる
- **MTP 非対応(v1 の割り切り)**: カメラ本体の USB 接続(MTPデバイス)は通常のファイルAPIで読めない。v1 は**ドライブレターが付くもの(カードリーダー経由)に限定**。MTP は将来課題

### コピー元の再帰走査

- 利用者が選択したコピー元フォルダを**コピー元ルート**と呼ぶ。スキャン対象は、コピー元ルート直下およびその全子孫フォルダにある対応ファイルとする
- 各ファイルについて、コピー元ルートからの相対ファイルパスと相対ディレクトリパスを保持する。たとえばコピー元ルートがSDカードのルート `E:\`、実ファイルが `E:\DCIM\100MSDCF\DSC00001.ARW` の場合、相対ファイルパスは `DCIM\100MSDCF\DSC00001.ARW`、相対ディレクトリパスは `DCIM\100MSDCF` とする
- コピー元ルート直下のファイルでは、相対ディレクトリパスを空文字とする
- 列挙後はコピー元ルートからの相対ファイルパスを、大文字・小文字を区別しない序数比較で昇順に並べる。ファイルシステムから返される列挙順には依存しない
- ディレクトリのシンボリックリンク、ジャンクションなどのリパースポイントは追跡しない。循環参照とコピー元ルート外の走査を防ぐ
- 読み取れないフォルダや、走査中に削除・抜去されたファイルはスキャンエラーとして記録し、利用者へ対象パスを表示する。カード全体を読み取れなくなった場合はスキャンを中止する
- コピー元とコピー先が同一、または一方が他方の配下になる組み合わせは禁止する。コピー結果を再帰走査で再びコピー元として検出することを防ぐ
- 対応拡張子によるファイル種別フィルタは、再帰列挙したすべてのファイルへ同じ規則で適用する

## 3. テンプレート仕様(v1方針確定)

詳細は [TEMPLATE_SPEC.md](TEMPLATE_SPEC.md) を正とする。

- トークン名は表示言語によらず英語の PascalCase で記述する
- パス区切り `\` を含めて、コピー先ルートからの相対パスを記述する
- 基本形は `{Token}`、書式対応トークンは `{Token:Format}`
- 既定テンプレートは `{TakenDate:yyyy-MM-dd}\{OriginalName}`
- v1 のファイルシステム系トークンは `{OriginalName}` `{FileName}` `{Extension}` `{SourceRelativeDirectory}` `{ModifiedDate}` `{FileSize}` `{Sequence}`
- v1 の Exif 系トークンは `{TakenDate}` `{TakenDateLocal}` `{TakenDateInTimeZone}` `{CameraMake}` `{CameraModel}` `{Lens}`
- Exif撮影日時は、タイムゾーン未考慮、PCのタイムゾーン、指定したタイムゾーンの3方式を選べる。指定には `JST` などアプリ定義の3文字コード、または `UTC+9` などUTCからの固定時差を使用する
- `{Sequence}` は競合がなければ空文字、競合時は既定3桁の `_001` から付与する。`{Sequence:4}` のように桁数を指定できる

トークンは2分類:

| 分類 | トークン例 | コスト |
|---|---|---|
| ファイルシステム系 | `{OriginalName}` `{FileName}` `{Extension}` `{SourceRelativeDirectory}` `{ModifiedDate:書式}` `{FileSize}` `{Sequence}` | `{Sequence}` の競合確認以外はゼロ(列挙時に取得済み) |
| Exif系 | `{TakenDate:書式}` `{TakenDateLocal:書式}` `{TakenDateInTimeZone:JST}` `{TakenDateInTimeZone:UTC+9}` `{CameraMake}` `{CameraModel}` `{Lens}` | ヘッダ読み込みが必要 |

- テンプレート解析時点で「Exif系トークンを含むか」が静的に判定できる → ファイルシステム系のみなら Exif を読まずに全宛先パスを即計算できる
- Exif が無いファイル(動画・一部RAW)では各撮影日時トークンを更新日時へフォールバックする
- 日時書式は .NET のカスタム日時書式を `InvariantCulture` で解釈する
- テンプレートから生成されるパスは必ず相対パスとし、絶対パス・親ディレクトリ参照・不正文字を拒否する
- `{SourceRelativeDirectory}` はコピー元ルートから元ファイル格納フォルダまでの相対パスを展開し、コピー元の複数階層をコピー先へ引き継げる。詳細な空文字時の区切り規則は `TEMPLATE_SPEC.md` に従う
- UI上のトークン説明、入力エラー、プレビュー状態は日本語・英語にローカライズする

## 4. 未取込判定の設計

基本方針: **テンプレートから宛先パスを計算し、宛先にファイルが存在して、コピー元の更新日時がコピー先と同じか古ければ取込済**。日時比較には `LastWriteTimeUtc` を使用する。

- 宛先フォルダの実ファイルを真実(source of truth)とする。ユーザーが宛先を手で整理しても、別PCでコピーしても破綻しない
- 「取込済台帳(コピー記録DB)」方式は、テンプレート変更や宛先の手動整理で実態とズレるため不採用

### Exif 系テンプレート使用時の高速化: Exif キャッシュ

基本フロー:

1. カード挿入 → ファイル列挙(速い)→ キャッシュヒット分は Exif 読み込みスキップ
2. キャッシュミス分のみ、`SemaphoreSlim` 等で上限を設けて並列にメタデータを読み込む。並列数は 1 / 2 / 4 / 8 を実機カードで計測し、既定値を決める

同じカードの使い回しでは2回目以降、新規撮影分しか Exif を読まない。初回 1000 枚を数秒〜十数秒で処理することを性能目標とし、進捗バーを表示する。ただし、実際の読込量と所要時間は JPEG/RAW/動画の形式、カード、カードリーダー、並列数に依存するため保証値とはしない。

#### キーと同一性判定

- エントリのキーは `(ボリュームシリアル番号, ボリュームルートからの相対パス, ファイルサイズ, LastWriteTimeUtc)` の**4要素すべて**とする。キーが完全一致した場合のみキャッシュヒットとし、不一致なら上書きせず別エントリとして追加する(追記型)。シリアルが異なる複数カードに同一パス・同一ファイル名のファイルが存在しても、互いのキャッシュを壊さない
- 相対パスの基準はコピー元ルートではなく**ボリュームルート**とする。ボリュームルートは `GetVolumePathNameW` で取得し、コピー元ルートの選び方(`E:\` と `E:\DCIM` など)が変わってもキーが変わらないようにする。相対パスは区切りを `\` に統一し、先頭の区切りを除去したうえで `StringComparer.OrdinalIgnoreCase` 相当で比較する。永続化時は元の表示用パスとは別に、大文字化した比較用キーを保持する
- 更新日時はファイル列挙時に取得した `LastWriteTimeUtc.Ticks` をそのまま保持・比較する。NTFS ではタイムゾーン・DST の影響を受けないが、ローカル時刻を保持する FAT 等では、PC のタイムゾーンや DST 設定の変更により取得される UTC 値が変わる場合がある。その場合は安全側にキャッシュミスとなり、Exif を再読み込みする
- ボリュームシリアル番号は `GetVolumeInformationW` の P/Invoke で取得する。シリアルはフォーマット時に付与されるため、**カードを再フォーマットすると別カードとして認識される**。これは仕様として割り切り、UI で「カードを再フォーマットすると新しいカードとして認識されます」と説明する
- ボリュームシリアル番号は 32bit であり、世界的に一意ではない。偶然の一致やセクタ単位クローンで重複した場合は同じカードとして扱う。さらに相対パス・サイズ・更新日時まで一致すると、内容が異なっていても誤った Exif を返す可能性が理論上は残るが、通常のカード利用では十分まれであり、ヘッダハッシュ等による追加照合は行わない。カード名・縮小版も同じ管理単位に混ざり得る。この制約は実用上十分として仕様上割り切る
- 値: 撮影日時(タイムゾーン変換前の壁時計値)・撮影日時オフセット(有効/欠落/不正の区別を含む)・カメラメーカー・カメラモデル・レンズなどの抽出済み Exif、更新日時へのフォールバック有無、埋め込みサムネイル有無と向き、エントリの最終利用日。キャッシュにはタイムゾーン変換後の値や生成済み宛先パスを保存せず、キャッシュヒット時もテンプレート評価と警告生成をやり直す
- キャッシュヒット時はエントリの最終利用日を更新し、スキャン完了時にまとめて書き出す
- キャッシュミスで Exif を読み込んだ場合は、読込後にファイルサイズと `LastWriteTimeUtc` を再取得する。読込前のキーから変化していたファイルはキャッシュへ保存せず、スキャン結果にも「走査中に変更された」エラーを表示する

#### 保存形式

- 保存場所は設定で変更可能な**キャッシュルート**とし、その直下に**ボリュームシリアル番号ごとに1フォルダ**を作る。既定のキャッシュルートは `Path.Combine(AppContext.BaseDirectory, "ExifCache")`、すなわち実行ファイルが置かれているフォルダ直下の `ExifCache` とする。`%LocalAppData%` 等、ユーザーから見えにくい場所への暗黙のフォールバックは行わない

```
<実行ファイルのフォルダ>\ExifCache\        ← 既定値。設定で任意の絶対パスへ変更可能
  A1B2C3D4\
    meta.json      … 利用者が付けた名前、ボリュームラベル、容量、ファイルシステム、
                     ドライブ種別、初回利用日、最終利用日、エントリ件数、
                     縮小版の対応表、スキーマバージョン
    entries.json   … Exif エントリ本体(数千件になりうる)、スキーマバージョン、抽出仕様バージョン
    thumb_{key-hash}.jpg
                   … 縮小版(埋め込みサムネイルの抜き出し)
```

- カード単位削除=フォルダ削除、キャッシュ容量表示=フォルダ内ファイルサイズ合計、と管理操作をファイル操作に1対1で対応させる
- 設定が「既定」の間は絶対パスを設定値として固定せず、起動ごとに `AppContext.BaseDirectory` から解決する。実行ファイル一式を別フォルダへ移動した場合、キャッシュルートも新しい実行ファイルの直下へ追従する。「既定に戻す」操作でも同じ規則を使用する
- カスタム保存先はフォルダ選択 UI で指定し、`Path.GetFullPath` で正規化した絶対パスとして保存する。設定画面とカード管理画面には、現在使用中のキャッシュルートの絶対パスを常に省略せず表示する
- 起動時は各カードの meta.json のみ読む。entries.json はスキャン対象ボリュームの分だけ遅延読み込みし、カードが増えても起動が遅くならないようにする
- 書き込みは対象ファイルと同じフォルダにプロセスごとに一意な一時ファイルを作成し、一時ファイル→置換で確定する(コピー処理と同じ流儀)。残留した一時ファイルは次回ロック取得時に削除する。JSON が壊れていた場合は破棄して再生成する(キャッシュであり、失っても Exif を再読み込みすれば取込判定の正しさに影響しない)
- meta.json と entries.json のスキーマバージョンは独立に持つ。entries.json には抽出仕様バージョンも持ち、抽出項目の追加だけでなく、MetadataExtractor の更新、タグの優先順位、日時・オフセットの解釈など抽出結果に影響する変更時に旧エントリを無効化できるようにする
- meta.json のエントリ件数・最終利用日と、一覧に表示するキャッシュ容量は派生情報として扱う。meta.json と entries.json の確定間でアプリが終了して不整合になった場合は、entries.json とフォルダ内の実ファイルから再計算して修復する
- 同じ相対パスのファイルがサイズ・更新日時を変えて再利用された場合も、旧キーのエントリは自動削除せず追記型で保持する。旧エントリはヒットしないため最終利用日が更新されず、管理画面の「最後に使われてから n 日」で利用者が削除できる。最終利用日は UTC 日付単位で更新し、同じ日にヒットしただけで entries.json 全体を書き直さない
- entries.json は保存時に有効エントリだけを再シリアライズし、明示的に削除済みのエントリや重複レコードを残さない。カード1枚あたりの件数・JSON サイズ・読込時間・保存時間を実測し、許容できない規模になった場合は §2 の方針どおり LiteDB 等への移行を検討する

#### 複数プロセスと排他制御

- アプリの複数起動を許可する。複数のカードリーダーを使い、異なるカードから同時に取り込めることを要件とする
- Exif キャッシュの排他制御には**ボリューム単位の名前付き Mutex**を使用する。Mutex 名はアプリ識別子、ユーザーごとのキャッシュルートを識別するハッシュ、8桁16進のボリュームシリアル番号から、Windows の名前制約に収まる決定的な文字列として生成する
- entries.json の読込前に Mutex を取得し、キャッシュ照合、Exif 読込、最終利用日の更新、meta.json / entries.json / 縮小版の確定がすべて完了するまで保持する。同じボリュームを対象にした複数プロセスは直列化し、古いスナップショットによる後勝ちの更新消失を防ぐ
- 異なるボリュームシリアル番号は異なる Mutex を使用するため、別カードのスキャン・取込は互いに待たず並行できる。管理画面からの名前変更、古いエントリの削除、カード単位削除、縮小版削除も対象ボリュームと同じ Mutex を取得して行う
- 待機中は「同じカードのキャッシュを別の PhotoImporter が使用しています」と表示し、スキャンのキャンセルを可能にする。Mutex の取得を待てない、または取得に失敗した場合、通常スキャンはキャッシュを読み書きせず Exif を直接読み込んで継続する。管理操作は実行せずエラーを表示する
- 所有プロセスの異常終了により放棄された Mutex を取得した場合は、キャッシュを必ずディスクから読み直し、残留一時ファイルと JSON の整合性を確認してから処理を続ける。Mutex は `finally` で確実に解放する

#### 障害時の扱い

- Exif キャッシュは性能最適化であり、コピー元写真とコピー先写真を正とする。ボリュームルート/シリアル取得失敗、キャッシュフォルダのアクセス拒否、JSON の破損、Mutex 取得失敗、ディスク容量不足、保存・置換失敗などが起きても、通常スキャンは警告を表示してキャッシュなしの Exif 読込へフォールバックし、可能な限り継続する
- 既定の実行ファイル直下を含め、選択中のキャッシュルートを作成・書込みできない場合は、実際のパスと理由を表示して保存先の変更を案内する。`%LocalAppData%`、一時フォルダ、レジストリ等へキャッシュ本体を黙って作成しない
- キャッシュルートがコピー元またはコピー先と同一、配下、あるいはその親に当たる場合、写真の走査対象への混入やコピー先の汚染を避けるため、そのスキャンではキャッシュを読み書きせず警告する。写真のスキャン自体は継続し、利用者に重ならない保存先への変更を案内する
- キャッシュ読込中に一部エントリだけが不正だった場合はそのエントリをミスとして扱う。ファイル全体を解釈できない場合は entries.json 全体を無効として再生成する。不正なキャッシュ値をテンプレート評価へ渡さない
- キャッシュの保存失敗は、そのスキャンでメモリ上に得た Exif とプレビューを無効にしない。次回スキャンが遅くなるだけとし、写真のコピー可否には影響させない
- 管理画面で利用者が明示した名前変更・削除は、失敗を成功扱いにしない。失敗対象と理由を表示し、設定や表示を実際のディスク状態へ戻す

#### カード管理画面

- キャッシュのあるカードを一覧表示する。表示項目: 名前、ボリュームラベル、容量、ドライブ種別、最終利用日、キャッシュ容量、エントリ件数、縮小版
- 一覧の上部に現在のキャッシュルートの絶対パスを表示する。保存先を変更して旧ルートにキャッシュを残した場合は「以前の保存先」としてその絶対パスと容量も保持・表示し、利用者がキャッシュの所在を見失わないようにする。通常スキャンが参照するのは現在のルートだけとする
- 利用者は目印となる名前を付けられる
- 現在コピー元に選択中のボリュームとシリアルが一致するカードを一覧上でハイライトする。ハイライトはキャッシュ設定 OFF でも動作する(シリアル照合のみで、キャッシュの読み書きを伴わないため)
- ドライブ種別(リムーバブル・固定・ネットワーク)はスキャン時に `GetDriveType` で記録し、カード以外のボリューム(HDD・NAS 等をコピー元にした場合)も区別して表示する。一覧からは除外せず、見えて消せる状態にする
- 削除は**手動のみ**とし、自動削除はしない:
  - カード単位の削除(フォルダごと)
  - カード内の「最後に使われてから n 日より古いエントリ」の削除。基準は撮影日時ではなく**エントリの最終利用日**とする。撮影日時基準では、カード上に残っている古い写真のエントリが削除→次回スキャンで再読み込み→再キャッシュのループになり、容量削減にならないため。UI 文言も「最後に使われてから n 日」であることが伝わる表現にする
- 削除確認では「キャッシュを消しても写真は消えない。次回スキャンが遅くなるだけ」を明記し、利用者が安心して消せるようにする

#### 縮小版(サムネイル)

- Exif 埋め込みサムネイル(IFD1 に格納されることが多い JPEG)を、メタデータ読み込みのついでに取得できた場合だけそのまま保存する。寸法、格納場所、圧縮形式はファイル・カメラごとに異なるため、IFD1 の 160×120 JPEG が必ず存在するとは仮定しない。画像本体や RAW 内の本画像・独自プレビューの自前デコードはしない
- **撮影日時の古い方 N 枚+新しい方 N 枚**を保持する。最初に撮った写真でどのカードかを覚えていることがあるため、古い方も持つ
- 各キャッシュエントリに埋め込みサムネイルの有無と Exif Orientation を保持する。スキャン完了時に「今回のスキャンに存在し、埋め込みサムネイル有りと判定済みのファイル」の中から候補を選び直す。候補に縮小版未保存のファイル(過去のスキャンでキャッシュ済みのため今回ヘッダを読まなかったファイル)が入った場合は、そのファイルだけヘッダを追加で再読み込みする。通常は最大 2N 件とする
- 埋め込みサムネイルを取れないファイル(動画など)は候補から除外する。有りと記録されていた候補から再読込時に取得できなかった場合は、そのエントリを「無し」に更新して次候補を補充するため、異常・ファイル変更時は再読込が 2N 件を超えることを許容する
- 古い方と新しい方の候補が重複した場合は重複排除する(枚数が少ないカードで起こる)
- 撮影日時が無いファイルは更新日時にフォールバックする(Exif 系トークンと同じ方針)
- 縮小版のファイル名は、ボリュームシリアルを除くエントリキーの正規化表現から SHA-256 で求めたハッシュを使い `thumb_{key-hash}.jpg` とする。meta.json にエントリキー、ファイル名、表示順、Orientation の対応表を保持し、WPF 表示時に Orientation に従って回転・反転する。候補から外れた縮小版と対応表にない孤立ファイルは、同じボリュームの Mutex 保持中に削除する
- 縮小版は写真の内容が PC 側に残ることを意味する。カードから写真を消しても縮小版は残るため、削除 UI(カード単位削除・「縮小版を保持」OFF 時の削除)で確実に消えることと対にする

#### 設定

- **「Exif キャッシュを利用する」**: OFF の場合、通常スキャンではキャッシュの読み取り・書き込み・メタ更新・Mutex 取得のすべてを行わない(読み取りだけ許すと、最終利用日の更新という書き込みが必要になり中途半端な状態になるため)。OFF でも管理画面では既存キャッシュの表示と削除を利用でき、その管理操作に限り対象ボリュームの Mutex とキャッシュファイルへアクセスする
- **「Exif キャッシュの保存先」**: 現在の絶対パス、フォルダ選択ボタン、「既定に戻す」ボタンを表示する。既定値は実行ファイルのフォルダ直下の `ExifCache`。変更候補は、既存ファイルを上書きしない一意な一時ファイルの作成・削除によって書込み可能性を確認してから適用する
- 保存先の変更は、その後のスキャンで使用するキャッシュルートを切り替える操作とする。v1 では既存キャッシュを新しい場所へ自動移動・自動削除しない。変更確認には旧ルートと新ルートの絶対パス、「旧保存先のキャッシュは残り、通常スキャンでは使われなくなる」ことを明記する
- 旧ルートは設定の「以前の保存先」一覧に記録し、カード管理画面から内容確認と削除を可能にする。旧ルートのキャッシュをすべて削除できた場合、またはフォルダが外部で削除済みと確認できた場合に一覧から除去する。データが存在する可能性があるパスを、確認なしで一覧から隠さない
- **「縮小版を保持する」**: キャッシュ設定に従属し、キャッシュ OFF 時は設定不可(グレーアウト)とする。ON→OFF に切り替えたときは、確認のうえ既存の縮小版も削除する(OFF にする動機はプライバシーである可能性が高く、過去のサムネイルが残り続けるのは期待に反するため)。全カードの Mutex を一度に保持せず、1ボリュームずつ取得・削除・解放する。削除に失敗したカードは一覧と理由を表示し、残存している縮小版を成功扱いにしない
- Exif 系トークンを含まないテンプレートでのスキャンは Exif を読まないため、キャッシュも縮小版も作成・更新しない。カードが管理画面に現れるのは Exif 系テンプレートで一度でもスキャンした後になる。**「最終利用日」は「キャッシュを最後に利用した日」と定義**し、この挙動と一貫させる

#### Exif キャッシュの実装前テスト条件

- キャッシュミスとヒットで、生成パス、フォールバック有無、警告コードが一致する
- 同じシリアルで相対パス・サイズ・更新日時のいずれかが異なるファイルは別エントリになり、4要素すべてが一致する場合だけヒットする。シリアル衝突時の割り切りもテスト名で明示する
- FAT 相当として列挙時の UTC Ticks が変化した場合は、安全にミスとなって再読込される
- Exif 読込中にサイズまたは更新日時が変わったファイルを保存せず、変更エラーにする
- 破損 JSON、アクセス拒否、保存失敗、Mutex 取得失敗では、通常スキャンがキャッシュなしで継続する。明示的な管理操作は失敗として表示する
- 既定値が実行ファイル直下へ追従すること、カスタム絶対パスの保存と「既定に戻す」、書込み禁止フォルダ、コピー元/コピー先との重複、保存先変更後の旧ルート表示を検証する
- 同一ボリュームを使う2プロセス相当のテストではキャッシュ更新が直列化され、異なるボリュームでは並行できる。放棄された Mutex の取得後に再読込・修復する
- 埋め込みサムネイル無し、候補重複、再読込失敗、Orientation 1〜8、候補から外れた縮小版、孤立した縮小版を検証する
- 実機の JPEG と主要 RAW のサンプルで、MetadataExtractor の抽出値、オフセット、サムネイル可否、初回/2回目の所要時間、並列数別の速度を計測する

### 判定の3状態

- **未取込**: 宛先に無い。`{Sequence}` がある場合は、必要に応じて既定3桁の `_001` から空いている番号を付与した宛先を使用する
- **取込済**: 宛先に有り、コピー元の更新日時がコピー先と同じか古い。ファイルサイズは表示と変更検知に使用するが、この判定では更新日時を優先する
- **競合**: コピー元の方が新しく、`{Sequence}` がなく、かつ上書きしない設定の場合。または指定された桁数で表現できる連番を使い切った場合

### コピー実行方針

- スキャン時に、表示する宛先相対パス、コピー元のサイズと `LastWriteTimeUtc`、コピー先の存在・サイズ・`LastWriteTimeUtc` をコピー計画として保持する
- コピー時にテンプレートを再評価したり、別の宛先を再割り当てしたりしない。利用者が確認したプレビューと実際のコピー先を一致させる
- コピー直前にはコピー計画の再計算ではなく、コピー元とコピー先がスキャン時から変わっていないかを再検証する。変化していたファイルはコピーせず、再スキャンが必要なエラーとして表示する
- UI に「既存ファイルを上書きする」設定を設ける。設定はスキャン前に選択し、プレビューの割り当てと状態へ反映する
  - `{Sequence}` がある: 上書き設定を参照しない。コピー元の方が新しくコピーが必要なら、常に次の連番候補を表示済み宛先として割り当てる
  - `{Sequence}` がない・上書きする: コピー元の方が新しければ、表示済みの同じ宛先へコピーする
  - `{Sequence}` がない・上書きしない: コピー元の方が新しければ競合とする
- コピー元の更新日時がコピー先と同じか古い場合は、上書き設定にかかわらずコピー対象外とする
- コピー元は削除・変更せず、ファイルは1件ずつ処理する

#### Windows API と一時ファイル

- ファイル内容のコピーには、Windows 8以降が提供する `CopyFile2` を使用する。対象OSはWindows 11のためフォールバックは設けず、アプリ独自のバッファ読み書きは実装しない
- コピー先と同じフォルダに `PI_<32桁のGUID>.partial` 形式の一時ファイルを作る。先頭にドットは付けず、`PI_` をアプリ識別子とする
- `COPYFILE2_EXTENDED_PARAMETERS` には `COPY_FILE_FAIL_IF_EXISTS`、キャンセル情報、進捗コールバック、コールバックコンテキストを指定する。一時名はGUIDで一意にするが、万一の衝突でも既存ファイルを上書きしない
- `COPYFILE2_CALLBACK_STREAM_FINISHED` を受けた時点で、メッセージの `Info.StreamFinished.hDestinationFile` に対して直ちに `FlushFileBuffers` を呼ぶ。ハンドルが有効なコールバック内で実行し、アプリ側では `CloseHandle`、保持・複製・再オープンをしない。これにより `CreateFileW`、読み取り専用属性の一時解除、再オープンに伴う時刻への懸念をなくす
- `FlushFileBuffers` が失敗した場合は、コールバックコンテキストへWin32エラーを保存して `COPYFILE2_PROGRESS_CANCEL` を返す。マネージ例外をネイティブコールバック境界の外へ送出しない。`CopyFile2` が返った後、保存したフラッシュエラーをファイル単位の失敗として報告し、正式名へ移さない
- `COPYFILE2_CALLBACK_CHUNK_FINISHED` の `uliTotalBytesTransferred` を全体バイト進捗へ使用する。利用者のキャンセル要求を検出した場合も `COPYFILE2_PROGRESS_CANCEL` を返し、Windowsにコピー断片の削除を要求する。`COPYFILE2_PROGRESS_STOP` と `COPYFILE2_PROGRESS_PAUSE` は使用しない
- `CopyFile2` の `S_OK` はコピー処理の完了を示すが、`FlushFileBuffers` を含めても記憶装置が要求を正しく実装しない場合や電源断に対する絶対保証とはみなさない。`COPY_FILE_NO_BUFFERING` は指定せず、各ストリーム完了時のフラッシュを使用する
- `CopyFile2` は拡張属性、OLE構造化ストレージ、NTFS代替データストリーム、ファイル属性を保持する一方、既定ではコピー元のセキュリティ属性をコピーしない。本アプリではコピー先フォルダ側のアクセス制御を適用したいので、この動作を採用し `COPY_FILE_REQUEST_SECURITY_PRIVILEGES` は指定しない
- `CopyFile2` が成功した後に一時ファイルのサイズと `LastWriteTimeUtc` をスキャン時のコピー元情報と照合してから正式名の確定へ進む。最終アクセス日時は取込判定やコピー検証には使用しない
- 一時ファイルへのコピー完了後、Windows の `MoveFileExW` で表示済みの正式名へ確定する。同じボリューム内で確定できるよう、一時ファイルは必ず正式なコピー先と同じフォルダに置く
- 上書きする場合は `MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH`、上書きしない場合は `MOVEFILE_WRITE_THROUGH` を使用する
- `ReplaceFileW` は使用しない。このAPIは置換前ファイルの作成日時、短いファイル名、オブジェクトID、DACL、暗号化・圧縮属性、名前付きストリームなどを置換ファイルへ引き継ぐ「既存ファイルの更新」に適している。本アプリでは、コピー元から作成した一時ファイルの内容とメタデータを正式な宛先として確定し、古いコピー先固有のメタデータを引き継がない方がコピー操作の意味に合う
- `ReplaceFileW` は置換先が存在する場合専用であり、新規確定には別APIが必要になる。また `REPLACEFILE_WRITE_THROUGH` はWindowsでサポートされておらず、一部の失敗コードでは置換元・置換先の名前や継承済みメタデータが部分的に変化し得る。新規確定と上書き確定の両方を `MoveFileExW` に統一して、分岐と復旧処理を単純にする
- `MOVEFILE_WRITE_THROUGH` を電源断に対する完全な確定保証とはみなさない。Windowsの公開仕様が明示しているフラッシュ保証は、移動がコピーと削除として実行される場合に対するものであり、同一ボリュームの置換を含む名前空間更新のクラッシュ原子性までは保証していない
- `MoveFileExW` が通常の実行中に失敗を返した場合は、正式ファイルと一時ファイルの両方を再確認する。正式ファイルがスキャン時の旧状態のままなら一時ファイルを安全条件に従って削除する。正式ファイルが無い、または状態を判定できない場合は一時ファイルを自動削除せず、復旧が必要なエラーとして残す
- 電源断・OSクラッシュではアプリが `MoveFileExW` の戻り値を受け取れないため、次回起動時に名前だけからコピーの成否を決めない。次の状態を確認して扱う
  - 正式ファイルだけがあり、サイズと更新日時がコピー元の期待値と一致する: コピー完了として扱う
  - スキャン時の旧正式ファイルと一時ファイルがある: 正式名の確定前に停止した可能性がある。コピー元を再スキャンし、必要なら元ファイルからコピーをやり直す
  - 正式ファイルと一時ファイルの両方がある: どちらが期待する内容かを名前だけでは判断せず、正式ファイルをコピー元と比較して再判定する
  - 正式ファイルがなく一時ファイルだけがある: 一時ファイルが唯一残ったコピー先データの可能性があるため、自動削除しない。また、一時名には本来の正式名やコピー元との対応情報がないため、自動的に正式名へ変更もしない。復旧が必要なエラーとして利用者へ表示する
  - 正式ファイルも一時ファイルも期待状態でない: コピー元が利用可能なら、コピー元を正として再スキャン・再コピーする
- 起動時に `PI_<32桁のGUID>.partial` を一括削除しない。「再スキャン・再コピー」とは、残った一時ファイルをコピー元として使うのではなく、削除していない元写真からコピー計画を作り直し、必要なファイルをもう一度コピーすることを指す。コピー元が利用できない場合は一時ファイルを保全し、自動処理せず利用者へ通知する
- `MoveFileExW` が成功を返した後も、正式ファイルが存在してスキャン時のコピー元サイズおよび更新日時と一致し、一時ファイルが存在しないことを確認してから、そのファイルを完了扱いにする
- キャンセルまたは失敗時に削除できるのは、現在のコピー処理が作成したパスとして記録され、コピー先ルート配下にあり、ファイル名が厳密に `^PI_[0-9A-Fa-f]{32}\.partial$` と一致する通常ファイルだけとする。`PI_` を含むだけのファイルや、利用者の通常ファイルは削除しない

#### 進捗とエラー

- 件数は「確定済みファイル数 / コピー対象ファイル数」を表示する
- バイト数は `CopyFile2` の `COPYFILE2_CALLBACK_CHUNK_FINISHED` を使い、「全完了ファイルの合計バイト数 + 現在コピー中の転送済みバイト数 / コピー対象の合計バイト数」を表示する
- 現在のファイル単独の進捗率は表示しない。UI には全体の件数と全体のバイト数だけを表示する
- キャンセル時は現在の Windows コピー処理を中止し、安全条件を満たす一時ファイルを削除する。確定済みファイルは残し、未処理ファイルには手を付けない
- ファイル単位のエラーは記録して、コピー元全体が利用不能になった場合を除き、残りの処理を続行する
- コピー完了後、一時ファイルと正式ファイルのサイズおよび `LastWriteTimeUtc` がコピー元のスキャン時情報と一致することを確認する。ハッシュ検証は仕上げ段階の追加候補とする

## 5. 画面イメージ

```
[コピー元フォルダ選択] [コピー先フォルダ選択]
[テンプレート入力欄] [スキャン]
[既存ファイルを上書きする] [コピー] [キャンセル]
─────────────────────────────
 対象  コピー元                →   コピー先(予定パス)      状態
  ☑   DCIM\100MSDCF\DSC00001.ARW → 2026-07-12\DSC00001.ARW 未取込
  ☐   DCIM\101MSDCF\DSC00002.ARW → 2026-07-11\DSC00002.ARW 取込済
─────────────────────────────
 [全体進捗バー]  完了件数 / 対象件数  転送済み / 合計バイト数
```

- 左右独立2ペインではなく、**1行に「元パス → 宛先パス → 状態」を並べる一覧**の方が対応関係が明確で実装も簡単
- コピー元欄にはファイル名だけでなく、コピー元ルートからの相対ファイルパスを表示する。異なるサブフォルダに同名ファイルが存在しても識別できるようにする
- テンプレートや上書き設定を変更した場合は、再スキャンしてコピー計画を更新する
- 数千行は WPF の UI 仮想化(VirtualizingStackPanel)で問題なし
- 状態フィルターは未実装。今後、未取込のみ・全件などの表示切替を追加する

## 6. 開発ステップ(1ステップずつ動作確認しながら進める)

1. **コアロジック先行(UIなし) — 実装済み**: テンプレートのパーサと宛先パス計算をクラスライブラリ+単体テストで作成。ファイルシステム系トークン、`{SourceRelativeDirectory}`、複数階層のパス評価、構造化エラーを実装
2. **最小UI — 実装済み**: WPF プロジェクト、フォルダ選択2つ、テンプレート入力、再帰走査、相対パス表示、リパースポイント非追跡、読み取りエラー、コピー元・コピー先の包含関係拒否を実装
3. **未取込判定(ファイルシステム系のみ) — 判定と表示は実装済み**: 宛先存在・更新日時チェックと、未取込・取込済・上書き対象・競合の4状態を実装。状態フィルターは未実装
4. **コピー実行 — 実装済み**: 対象選択、上書き設定、`CopyFile2` による逐次コピー、件数・バイト進捗、キャンセル、ファイル単位のエラー継続、一時ファイル、コピー前後検証、`MoveFileExW` による確定、コピー後再スキャンを実装
5. **Exif 対応 — 実装済み、実データ検証は継続**: MetadataExtractor 2.9.3、`{TakenDate}` 等のExif系トークン、Exifオフセットを使うタイムゾーン変換、更新日時フォールバック、メタデータ値のサニタイズ、プレビュー警告、Exifスキャンの件数・進捗率表示を実装。JPEG と主要 RAW の実データ検証は継続する
6. **Exif キャッシュ — 未実装**: 設定可能なキャッシュルート(既定は実行ファイル直下の `ExifCache`)と、ボリュームシリアル番号ごとのフォルダ + JSON キャッシュで2回目以降を高速化。ボリューム単位の名前付き Mutex、障害時のキャッシュなしフォールバック、カード管理画面(保存先表示・以前の保存先・名前付け・補助情報・挿入中カードのハイライト・縮小版・手動削除)と設定(キャッシュ利用・保存先・縮小版保持)を実装。複数プロセス、実機写真、並列数、JSON 規模を実測して確認
7. **仕上げ — 未実装**: 設定保存(前回のコピー元/コピー先フォルダ・テンプレート・上書き設定。Exif キャッシュ関連設定はステップ6で先行実装)、テンプレートプリセット、任意のハッシュ検証、状態フィルター、多言語化、単一 exe 化(Costura)の組み込み

2026-07-14時点で Core の単体テストは56件あり、すべて成功している。

## 7. 次のアクション

- 実機の写真・大容量ファイル・カード抜去を使い、コピー、キャンセル、ファイル単位のエラー継続、一時ファイル復旧案内を確認する
- 状態フィルターを追加する
- ステップ5の実データ検証では、JPEG と主要 RAW ごとに日時・オフセット・埋め込みサムネイル・Orientation・読込量を確認し、ステップ6のキャッシュエントリ項目を確定する
- 実データ検証後、ステップ6としてExifキャッシュのキー・抽出結果モデル・ボリューム情報取得から実装する
- 起動時に残存する `PI_<GUID>.partial` を検出し、無条件削除や自動昇格をせず利用者へ通知する
